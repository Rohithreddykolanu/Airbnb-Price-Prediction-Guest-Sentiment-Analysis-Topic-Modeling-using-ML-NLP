# -*- coding: utf-8 -*-
"""Airbnb Price Prediction, Guest Sentiment Analysis & Topic Modeling using ML, NLP & Neural Networks.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1geMXk51DFyWAV6dcfk36-DBDjBxm16P2

# ***Airbnb Price Prediction, Guest Sentiment Analysis & Topic Modeling using ML, NLP & Neural Networks***
---

## ***Problem Statement***
---
The first objective of this project is to conduct a Guest Experience Analysis using Natural Language Processing (NLP) techniques on Airbnb review data from **`Chicago, Illinois`**. Online reviews provide valuable unstructured insights into guest perceptions of cleanliness, service quality, location convenience, and overall satisfaction. Through systematic text preprocessing, sentiment analysis, zero-shot classification, and topic modeling qualitative review content will be transformed into measurable indicators of customer experience while also identifying the key themes frequently discussed by guests. The analysis will examine sentiment patterns across amenities and property types and evaluate their relationship with listing performance. The findings aim to provide actionable insights for hosts to improve service quality and strengthen their competitive position in the marketplace.

The second objective of this project is to develop a neural networks and machine learning model to predict Airbnb listing prices in **`Chicago, Illinois, United States`** using structured property characteristics and indicators derived from guest reviews. Pricing plays a critical role in revenue optimization, occupancy performance, and competitive positioning in short-term rental markets. Using the Listings dataset, variables such as property type, room type, and host attributes will be incorporated into a regression framework to identify the key determinants of listing prices. In addition, sentiment scores, zero-shot classification scores, and topic-based scores derived from the reviews dataset using sentiment analysis, zero-shot classification and LDA topic modeling will be incorporated into the price prediction model to evaluate whether guest perceptions, experiences, and latent thematic patterns improve predictive accuracy and influence listing valuation.

## ***Data Dictionary of Listings Dataset***
---

### Listing Identification & Metadata
1. **`id:`** Unique identifier for each listing
2. **`listing_url:`** Direct URL to the Airbnb listing page
3. **`scrape_id:`** Identifier for the dataset scrape instance
4. **`last_scraped:`** Date when listing information was last scraped
5. **`source:`** Data source reference
6. **`name:`** Listing title
7. **`description:`** Detailed property description
8. **`neighborhood_overview:`** Host-provided description of the neighborhood
9. **`picture_url:`** URL of listing's main image

### Host Information
10. **`host_id:`** Unique identifier for host
11. **`host_url:`** Host profile URL
12. **`host_name:`** Host name
13. **`host_since:`** Date host joined Airbnb
14. **`host_location:`** Host's reported location
15. **`host_about:`** Host's profile description
16. **`host_response_time:`** Typical response time (e.g., within an hour)
17. **`host_response_rate:`** Percentage of messages responded to
18. **`host_acceptance_rate:`** Percentage of booking requests accepted
19. **`host_is_superhost:`** Indicates if host has Superhost status
20. **`host_thumbnail_url:`** Host profile thumbnail image
21. **`host_picture_url:`** Host profile image
22. **`host_neighbourhood:`** Host's neighborhood
23. **`host_listings_count:`** Total listings owned by host
24. **`host_total_listings_count:`** Total listings including inactive
25. **`host_verifications:`** Verification methods (email, phone, etc.)
26. **`host_has_profile_pic:`** Indicates if host has a profile picture
27. **`host_identity_verified:`** Indicates if host identity is verified

### Location Variables
28. **`neighbourhood:`** Raw neighborhood name
29. **`neighbourhood_cleansed:`** Standardized neighborhood name
30. **`neighbourhood_group_cleansed:`** Larger regional grouping
31. **`latitude:`** Latitude coordinate
32. **`longitude:`** Longitude coordinate

### Property Characteristics
33. **`property_type:`** Type of property (Apartment, House, etc.)
34. **`room_type:`** Type of room (Entire home, Private room, Shared room)
35. **`accommodates:`** Maximum number of guests
36. **`bathrooms:`** Number of bathrooms
37. **`bathrooms_text:`** Bathroom description in text form
38. **`bedrooms:`** Number of bedrooms
39. **`beds:`** Number of beds
40. **`amenities:`** List of amenities offered

### Pricing & Stay Requirements
41. **`price:`** Nightly listing price
42. **`minimum_nights:`** Minimum nights required
43. **`maximum_nights:`** Maximum nights allowed
44. **`minimum_minimum_nights:`** Lowest observed minimum nights
45. **`maximum_minimum_nights:`** Highest observed minimum nights
46. **`minimum_maximum_nights:`** Lowest observed maximum nights
47. **`maximum_maximum_nights:`** Highest observed maximum nights
48. **`minimum_nights_avg_ntm:`** Avg. minimum nights (next 12 months)
49. **`maximum_nights_avg_ntm:`** Avg. maximum nights (next 12 months)

### Availability & Calendar Information
50. **`calendar_updated:`** Calendar update frequency
51. **`has_availability:`** Indicates availability status
52. **`availability_30:`** Available days in next 30 days
53. **`availability_60:`** Available days in next 60 days
54. **`availability_90:`** Available days in next 90 days
55. **`availability_365:`** Available days in next 365 days
56. **`calendar_last_scraped:`** Date calendar was last scraped
57. **`number_of_reviews:`** Total number of reviews
58. **`number_of_reviews_ltm:`** Reviews in last 12 months
59. **`number_of_reviews_l30d:`** Reviews in last 30 days
60. **`availability_eoy:`** Availability at end of year

### Review & Performance Metrics
61. **`number_of_reviews_ly:`** Reviews last year
62. **`estimated_occupancy_l365d:`** Estimated occupancy rate (last 365 days)
63. **`estimated_revenue_l365d:`** Estimated revenue (last 365 days)
64. **`first_review:`** Date of first review
65. **`last_review:`** Date of most recent review

### Review Scores (Ratings)
66. **`review_scores_rating:`** Overall rating score
67. **`review_scores_accuracy:`** Rating for accuracy
68. **`review_scores_cleanliness:`** Rating for cleanliness
69. **`review_scores_checkin:`** Rating for check-in experience
70. **`review_scores_communication:`** Rating for communication
71. **`review_scores_location:`** Rating for location
72. **`review_scores_value:`** Rating for value

### Legal & Booking Features
73. **`license:`** License/registration number
74. **`instant_bookable:`** Whether listing allows instant booking
75. **`calculated_host_listings_count:`** Total listings by host
76. **`calculated_host_listings_count_entire_homes:`** Host's entire-home listings
77. **`calculated_host_listings_count_private_rooms:`** Host's private-room listings
78. **`calculated_host_listings_count_shared_rooms:`** Host's shared-room listings
79. **`reviews_per_month:`** Average reviews per month

---

## ***Data Dictionary of Reviews Dataset***
---
1. **`listing_id:`**	Unique identifier of the Airbnb listing associated with the review. Used to link reviews with the Listings dataset (id).
2. **`id:`**	Unique identifier for each individual review.
3. **`date:`**	Date when the review was posted.
4. **`reviewer_id:`**	Unique identifier of the guest who wrote the review.
5. **`reviewer_name:`**	Name of the guest who posted the review.
6. **`comments:`**	Full textual content of the guest review. This field contains unstructured text used for sentiment analysis and NLP tasks.

---

**`Source:`** *[Inside Airbnb](https://insideairbnb.com/get-the-data/)*

---

### Import the required libraries
"""

# Numerical and data manipulation libraries
import numpy as np
import pandas as pd

# Data visualization libraries
import matplotlib.pyplot as plt
import seaborn as sns
import plotly.express as px

# Data preprocessing and model selection
from sklearn.model_selection import train_test_split

# Deep learning and neural network libraries
import tensorflow as tf
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import Dense, Normalization, Input, Dropout, BatchNormalization
from tensorflow.keras.regularizers import l2
from tensorflow.keras.callbacks import EarlyStopping
from tensorflow.keras.optimizers import Adam
from sklearn import metrics
from sklearn.metrics import mean_squared_error, mean_absolute_error, r2_score

# Model evaluation metrics
sns.set(style="whitegrid")

# Suppress warnings
import warnings
warnings.filterwarnings('ignore')

"""### Data loading of Airbnb Datasets (Listings and Reviews)"""

# Import Airbnb Listings Dataset
listings_url = 'https://data.insideairbnb.com/united-states/il/chicago/2025-09-22/data/listings.csv.gz'

# Load the Dataset into DataFrame
listings_df = pd.read_csv(listings_url, compression = 'gzip')

# Import Airbnb Reviews Dataset
reviews_url = 'https://data.insideairbnb.com/united-states/il/chicago/2025-09-22/data/reviews.csv.gz'

# Load the Dataset into DataFrame
reviews_df = pd.read_csv(reviews_url, compression = 'gzip')

"""### Initial exploration of Reviews Dataset


"""

# Display the first five rows of the Reviews Dataset
reviews_df.head()

# Display the Shape of Reviews Dataset
reviews_df.shape

"""***`Observations:`***

- The reviews dataset contains 492465 rows and 6 columns, indicating a large volume of Airbnb guest review data.

- The dataset includes important review level information such as listing_id, review id, date, reviewer_id, reviewer_name, and comments.

- The comments column contains unstructured text data, which is essential for NLP based analysis such as sentiment analysis, language detection, topic modeling, and guest experience classification.

- The initial records show that multiple reviews are linked to the same listing, which means review data can later be aggregated at the listing level for integration with the listings dataset.

- The presence of non English comments in the sample indicates that language detection or filtering is necessary before applying English based NLP models.

### Initial Sanity Checks of the Reviews Dataset
"""

# Check for the missing values
reviews_df.isnull().sum()

# Drop the rows where there are missing values in the comments column
reviews_df.dropna(subset = 'comments', inplace = True)

# Check the shape of the reviews after dropping the missing values
reviews_df.shape

"""***`Observations:`***

- The missing value check shows that all columns in the reviews dataset have zero missing values except the comments column, which contains 164 missing records.

- Since the comments column is the main text feature used for NLP tasks, removing these missing records is necessary to avoid errors during text preprocessing, sentiment analysis, and topic modeling.

- After dropping the missing values from the comments column, the dataset contains 492301 rows and 6 columns, which indicates that only a very small portion of the data was removed.

- The dataset remains large and reliable for further analysis because the removal of 164 records does not significantly reduce the overall review volume.

### ***Time Series Plot (Reviews per Year)***
"""

# Convert review dates into datetime format
reviews_df['date'] = pd.to_datetime(reviews_df['date'], errors = 'coerce')

# Extract review year from the date column
reviews_df['year'] = reviews_df['date'].dt.year

# Calculate total number of reviews for each year
reviews_per_year = reviews_df.groupby('year').size().reset_index(name = 'Total Reviews')

# Create line plot to show yearly review trends
plt.figure(figsize = (15, 7))
sns.lineplot( data = reviews_per_year, x = 'year', y = 'Total Reviews', marker = 'o', color = 'green')

# Add review count labels to each data point
for x, y in zip(reviews_per_year['year'], reviews_per_year['Total Reviews']):
    plt.text(x, y, str(y), ha='left', va='top')

# Add chart title and axis labels
plt.title('Total Reviews per Year')
plt.xlabel('Year')
plt.ylabel('Total Reviews')

# Format x-axis labels for readability
plt.xticks(rotation = 45)
plt.tight_layout()

# Display grid and final plot
plt.grid()
plt.show()

"""***`Observations:`***

- The number of Airbnb reviews increased steadily from 2009 to 2019, rising from only 8 reviews in 2009 to 37196 reviews in 2019.

- There was a sharp decline in 2020, with reviews dropping to 18772, which suggests a major disruption in Airbnb activity during that year.

- After 2020, the review volume recovered strongly, increasing to 41670 in 2021, 62347 in 2022, 78384 in 2023, and reaching the highest value of 102132 reviews in 2024.

- The slight decrease to 91355 reviews in 2025 may indicate incomplete data for the year or a moderation in review activity after the peak observed in 2024.

- Overall, the trend shows strong long term growth in Airbnb review activity in Chicago, making the dataset suitable for analyzing guest experience patterns over time.

### Filtering the most recent Reviews
"""

# Filter only reviews from the year 2023, 2024, 2025
reviews_df = reviews_df[(reviews_df['date'].dt.year.isin([2023, 2024, 2025]))].copy()

# Check the shape of the filtered reviews
reviews_df.shape

# Count unique Airbnb listings in the reviews dataset
reviews_df['listing_id'].nunique()

"""***`Observations:`***

- The reviews dataset was filtered to include only the most recent reviews from 2023, 2024, and 2025.

- After filtering, the dataset contains 271871 rows and 7 columns, indicating that a large number of recent reviews are available for analysis.

- The filtered dataset includes reviews from 6705 unique Airbnb listings, which provides broad coverage across multiple properties in Chicago.

- Using recent reviews makes the guest experience analysis more relevant because it reflects current customer perceptions, service quality, listing conditions, and market trends.

### Aggregate Reviews at the Listing Level
"""

# Sort reviews by listing_id and date (latest first), then keep top 20 reviews per listing
reviews_df = (
    reviews_df.sort_values(by = ['listing_id', 'date'], ascending = [True, False])
    .groupby('listing_id')
    .head(20)
)

# Combine all selected reviews into a single text per listing
reviews_df = (
    reviews_df.groupby('listing_id')['comments']
    .apply(lambda x: ' '.join(x.astype(str)))
    .reset_index(name = 'comments')
)

# Get the dimensions of the dataframe (rows, columns)
reviews_df.shape

"""***`Observations:`***

- The reviews were sorted by listing_id and review date, with the most recent reviews selected first for each listing.

- Only the latest 20 reviews were retained for each listing, which helps maintain recent and relevant guest feedback while reducing excessive text volume.

- The selected reviews were aggregated into a single combined text field for each Airbnb listing.

- After aggregation, the dataset contains 6705 rows and 2 columns, where each row represents one unique listing with its combined review comments.

- This listing level aggregation is useful for connecting guest review insights with structured listing data for sentiment analysis, topic modeling, zero shot classification, and price prediction.

### Text Pre-Processing
"""

# Import required libraries for the Text Pre-Processing
import re                                # For regular expression operations (text cleaning and pattern matching)
import nltk                              # Natural Language Toolkit for NLP tasks
from nltk.corpus import stopwords        # To remove common words that add little meaning
from nltk.stem import WordNetLemmatizer  # For reducing words to their base form (lemmatization)
import string                            # For handling punctuation and string operations

# Download required NLTK data
nltk.download('stopwords')  # Stopword corpus for removing common words
nltk.download('wordnet')    # Lexical database for lemmatization
nltk.download('omw-1.4')    # Multilingual WordNet data (supports lemmatizer)
nltk.download('punk_tab')   # Tokenizer models for splitting text into words/sentences

# Initialize lemmatizer for reducing words to their base form
lemmatizer = WordNetLemmatizer()

# Define text preprocessing function for Airbnb review comments
def preprocess_text(text):
  if pd.isnull(text):
    return ''

  # Remove URLs, special characters, and convert text to lowercase
  text = re.sub(r'http\S+', '', text)
  text = re.sub(r'[^a-zA-Z\s]', '', text)
  text = text.lower()

  # Tokenize text using simple whitespace splitting
  tokens = text.split()

  # Define custom stopwords specific to Airbnb and Chicago review context
  custom_stopwords = {
    'stay', 'stayed', 'staying', 'host', 'hosts', 'place', 'places', 'property', 'apartment', 'apt', 'cub', 'wrigleyville',
    'home', 'house', 'room', 'bedroom', 'bathroom', 'space', 'area', 'location', 'neighborhood', 'city', 'la', 'muy', 'de',
    'great', 'good', 'nice', 'amazing', 'awesome', 'perfect', 'excellent', 'wonderful', 'comfortable', 'clean', 'und',
    'really', 'very', 'quite', 'much', 'lot', 'recommend', 'enjoy', 'love', 'like', 'overall', 'wrigley', 'milan',
    'chicago', 'downtown', 'loop', 'park', 'lincoln', 'riverwalk', 'navy', 'pier', 'millennium', 'cta'
    }

  # Combine standard English stopwords with custom project-specific stopwords
  stop_words = set(stopwords.words('english')).union(custom_stopwords)

  # Lemmatize words and remove stopwords and punctuation
  tokens = [lemmatizer.lemmatize(word) for word in tokens if word not in stop_words and word not in string.punctuation]

  # Return cleaned review text
  return ' '.join(tokens)

# Apply text preprocessing to Airbnb review comments
reviews_df['comments'] = reviews_df['comments'].apply(preprocess_text)

"""***`Observations:`***

- The text preprocessing function standardizes Airbnb review comments by removing URLs, special characters, punctuation, and converting all text to lowercase.

- The use of lemmatization helps reduce words to their base form, which improves consistency in text analysis and reduces vocabulary size.

- Standard English stopwords are combined with custom Airbnb and Chicago specific stopwords to remove commonly repeated words that may not add meaningful analytical value.

- The preprocessing step prepares the review text for NLP tasks such as sentiment analysis, topic modeling, zero shot classification, and feature extraction.

- By applying this function to the comments column, the dataset becomes cleaner and more suitable for extracting meaningful guest experience patterns.

### HTML parsing or HTML tag removal
"""

# HTML parsing and entity decoding libraries
from bs4 import BeautifulSoup
import html

# Define function to clean HTML content from review comments
def clean_html(text):

    # Handle missing or non-text values
    if not isinstance(text, str):
        return ''

    # Decode HTML entities and remove HTML tags
    text = html.unescape(text)
    text = BeautifulSoup(text, "html.parser").get_text(separator=' ')

    # Return cleaned text without extra spaces
    return text.strip()

# Apply HTML cleaning to Airbnb review comments
reviews_df['comments'] = reviews_df['comments'].apply(clean_html)

"""***`Observations:`***

- The HTML cleaning function removes HTML tags and decodes HTML entities from the Airbnb review comments.

- The function also handles missing or non text values by replacing them with empty strings, which prevents errors during text processing.

- This step is important because review comments may contain encoded characters or HTML fragments that can affect NLP model performance.

- After applying HTML cleaning, the comments become more readable and consistent for sentiment analysis, topic modeling, and zero shot classification.

- Overall, this preprocessing step improves the quality of the review text before extracting guest experience insights.

### Keeping only English Reviews using Language Detection (Hugging Face)
"""

# Transformer pipeline for language detection
from transformers import pipeline

# Initialize multilingual language detection model
lid = pipeline(
    'text-classification',
    model = 'papluca/xlm-roberta-base-language-detection',
)

# Convert review comments into string format
texts = reviews_df['comments'].astype(str)

# Predict language labels for review comments in batches
preds = lid(
    texts.tolist(),
    batch_size = 64,
    truncation = True,
    max_lenght = 512
)

# Extract predicted language labels and confidence scores
labels = [p.get('label', '') for p in preds]
scores = [float(p.get('scores', 0.0)) for p in preds]

# Store language detection results in the reviews dataframe
reviews_df['language'] = labels
reviews_df['score'] = scores

# Display updated reviews dataframe
reviews_df.head()

# Count the number of reviews for each detected language
lang_counts = reviews_df['language'].value_counts()

# Calculate the percentage distribution of each language
lang_percentages = (lang_counts / len(reviews_df)) * 100

# Create a summary dataframe with language counts and percentages
lang_summary = pd.DataFrame({
    'Count': lang_counts,
    'Percentage': lang_percentages.round(2)
})

# Display language distribution summary
lang_summary

# Keep only English-language reviews for NLP analysis
reviews_df = reviews_df[reviews_df['language'] == 'en'].copy()

# Remove language detection columns after filtering
reviews_df.drop(columns = ['language', 'score'], inplace = True)

# Check the number of rows and columns after all text pre-processing steps
reviews_df.shape

"""***`Observations:`***

- The language detection model identified English as the dominant language in the reviews dataset, with 5982 records representing 89.22 percent of the total reviews.

- Spanish is the second most frequent language with 267 reviews, followed by German with 123 reviews and French with 100 reviews.

- Several other languages such as Italian, Thai, Portuguese, Dutch, Swahili, Turkish, Polish, Vietnamese, Urdu, and Hindi appear in smaller proportions.

- Since most NLP models used in this project are better suited for English text, filtering the dataset to only English reviews improves the consistency and reliability of sentiment analysis, topic modeling, and zero shot classification.

- After keeping only English language reviews and removing the language detection columns, the final processed reviews dataset contains 5982 rows and 2 columns.

- This filtered dataset provides a cleaner and more focused text corpus for extracting guest experience insights from English Airbnb reviews.

### ***Sentiment Analysis with VADER***
"""

# Install vaderSentiment
!pip install vaderSentiment

# VADER sentiment analysis library
from vaderSentiment.vaderSentiment import SentimentIntensityAnalyzer

# Initialize VADER sentiment analyzer
analyzer = SentimentIntensityAnalyzer()

# Define function to extract VADER sentiment scores
def get_vader_scores(text):

  # Calculate sentiment scores for each review comment
  scores = analyzer.polarity_scores(str(text))

  # Return compound, positive, neutral, and negative sentiment scores
  return pd.Series([scores['compound'], scores['pos'], scores['neu'], scores['neg']])

# Create a copy of the reviews dataset for VADER sentiment analysis
vader_sentiment_reviews_df = reviews_df.copy()

# Apply VADER sentiment scoring to review comments
vader_sentiment_reviews_df[['vader_compound', 'vader_positive', 'vader_neutral', 'vader_negative']] = vader_sentiment_reviews_df['comments'].apply(get_vader_scores)

# Classify reviews into positive, neutral, or negative sentiment categories
vader_sentiment_reviews_df['vader_sentiment'] = vader_sentiment_reviews_df['vader_compound'].apply(lambda x:
    'POSITIVE' if x >= 0.5
    else 'NEGATIVE' if x <= -0.5
    else 'NEUTRAL'
    )

# Display reviews with VADER sentiment scores and labels
vader_sentiment_reviews_df.head()

# Save dataframe as CSV
vader_sentiment_reviews_df.to_csv('vader_sentiment_reviews_df.csv', index = False)

# Download the CSV file
from google.colab import files
files.download('vader_sentiment_reviews_df.csv')

# Count the number of reviews in each VADER sentiment category
vader_sentiment_reviews_df['vader_sentiment'].value_counts()

"""***`Observations:`***

- VADER sentiment analysis classified the majority of Airbnb reviews as positive, with 5621 reviews falling under the positive category.

- Only 69 reviews were classified as negative and 292 reviews were classified as neutral, showing that negative and neutral guest experiences represent a very small portion of the dataset.

- The sample output shows high compound sentiment scores for the displayed reviews, indicating strong positive sentiment in those comments.

- This result suggests that most guests expressed favorable experiences in their Airbnb reviews, which may reflect satisfaction with factors such as host communication, comfort, safety, cleanliness, or overall stay quality.

- The VADER sentiment scores provide useful numerical features that can be used to measure guest perception and later support listing level price prediction analysis.

### ***Sentiment Analysis with Hugging Face Transformers***
"""

# Transformer pipeline for sentiment analysis
from transformers import pipeline

# Initialize default transformer-based sentiment analysis model
sentiment_analyzer = pipeline('sentiment-analysis')

# Create a copy of the reviews dataset for transformer-based sentiment analysis
huggingface_sentiment_reviews_df = reviews_df.copy()

# Apply Hugging Face sentiment analysis to review comments
huggingface_sentiment_reviews_df[['huggingface_score', 'huggingface_sentiment']] = huggingface_sentiment_reviews_df['comments'].apply(
    lambda x: pd.Series({

        # Store sentiment confidence score for each review
        'huggingface_score': sentiment_analyzer(x[:512])[0]['score'],

        # Store predicted sentiment label for each review
        'huggingface_sentiment': sentiment_analyzer(x[:512])[0]['label']
    })
)

# Display reviews with Hugging Face sentiment scores and labels
huggingface_sentiment_reviews_df.head()

# Save dataframe as CSV
huggingface_sentiment_reviews_df.to_csv('huggingface_sentiment_reviews_df.csv', index = False)

# Download the CSV file
from google.colab import files
files.download('huggingface_sentiment_reviews_df.csv')

# Count the number of reviews in each Hugging Face sentiment category
huggingface_sentiment_reviews_df['huggingface_sentiment'].value_counts()

"""***`Observations on Hugging Face Sentiment Analysis:`***

- The Hugging Face transformer model classified 4,065 reviews as positive and 1,917 reviews as negative, showing that the majority of guest reviews reflect a favorable Airbnb experience.

- The sentiment score provides a confidence value for each review’s predicted sentiment, which helps quantify how strongly a review is positive or negative rather than only using a text label.

- Positive reviews often include words related to helpful hosts, good location, comfort, and overall satisfaction, while negative reviews may reflect issues related to shared spaces, expectations, or guest inconvenience.

- Since guest sentiment captures the quality of customer experience from review text, it can be useful as an additional numerical feature for understanding how guest perception relates to listing performance.

- Overall, incorporating Hugging Face sentiment scores adds an NLP-based customer experience signal to the dataset, helping the model learn patterns beyond traditional listing attributes such as price, location, amenities, and reviews.

## ***Zero-Shot Classification***
"""

# Transformer and GPU acceleration libraries
from transformers import pipeline
import torch

# Initialize zero-shot classifier for review theme detection
classifier = pipeline(
    'zero-shot-classification',
    model = 'typeform/distilbert-base-uncased-mnli',
    device = 0 if torch.cuda.is_available() else -1,
    torch_dtype = torch.float16 if torch.cuda.is_available() else None
)

# Define guest experience categories for zero-shot classification
categories = [
    'Location',
    'Amenities',
    'Service',
    'Cleanliness',
    'Safety'
]

# Apply zero-shot classification to identify review themes
results = classifier(
    reviews_df['comments'].astype(str).tolist(),
    candidate_labels = categories,
    multi_label = True
)

# Convert zero-shot classification results into a structured dataframe
zero_shot_df = pd.DataFrame([{

    # Store listing ID associated with each review
    'review_id': reviews_df.iloc[i]['listing_id'],

    # Store classification scores for each guest experience category
    **{label: score for label, score in zip(res['labels'], res['scores'])}
} for i, res in enumerate(results)])

# Display zero-shot classification scores for review themes
zero_shot_df.head()

# Save dataframe as CSV
zero_shot_df.to_csv('zero_shot_df.csv', index = False)

# Download the CSV file
from google.colab import files
files.download('zero_shot_df.csv')

"""***`Observations on Zero-Shot Classification Scores:`***

- The zero-shot classification model successfully converts unstructured guest review text into structured numerical theme scores for key Airbnb experience factors such as Cleanliness, Safety, Amenities, Location, and Service.

- Most sample reviews show very high scores across multiple categories, indicating that guest comments often mention several aspects of the stay at the same time rather than focusing on only one theme.

- Cleanliness, Amenities, and Location appear consistently strong in the displayed records, suggesting that these factors are frequently reflected in guest feedback and may carry useful information about listing quality.

- Since multi_label=True is used, each review can belong to more than one category, which is suitable for Airbnb reviews because guests usually discuss multiple experience dimensions in a single comment.

- These zero-shot scores will be merged with the listings dataset as additional feature columns, allowing the neural network model to learn how guest experience themes relate to Airbnb listing price.

## ***Topic Modeling***
"""

# Import required libraries for Topic Modeling
from wordcloud import WordCloud

# Tokenization library for splitting text into words
from nltk.tokenize import word_tokenize

# Text vectorization libraries for topic modeling
from sklearn.feature_extraction.text import CountVectorizer, TfidfVectorizer

# Topic modeling and dimensionality reduction models
from sklearn.decomposition import LatentDirichletAllocation, NMF, TruncatedSVD

"""### Convert Text to Numeric Form (Bag-of-Words)"""

# Create Bag-of-Words vectorizer for review comments
bow_vectorizer = CountVectorizer(
    min_df = 5,
    max_df = 0.90,
    stop_words = 'english',
    ngram_range = (1, 3)
)

# Transform cleaned review comments into a Bag-of-Words matrix
x_bow = bow_vectorizer.fit_transform(reviews_df['comments'])

# Extract feature names from the Bag-of-Words vocabulary
bow_feature_names = bow_vectorizer.get_feature_names_out()

# Convert Bag-of-Words matrix into a dataframe for readability
bow_df = pd.DataFrame(x_bow.toarray(), columns = bow_feature_names)

# Display Bag-of-Words matrix dimensions and sample records
print('BoW matrix shape: ', x_bow.shape)
bow_df.head()

"""***`Observations:`***

- The Bag of Words vectorizer transformed 5982 cleaned review comments into a structured text matrix with 50472 features.

- The large number of features shows that the reviews contain a wide vocabulary, including single words, two word phrases, and three word phrases.

- The use of min_df = 5 removes very rare terms, while max_df = 0.90 removes extremely common terms that may not add much analytical value.

- The matrix contains mostly zero values because each review uses only a small portion of the full vocabulary.

- This representation is useful for topic modeling because it converts unstructured review text into numerical features that can be used to identify common themes across guest reviews.

### Convert Text to Numeric Form (TF-IDF)
"""

# Create TF-IDF vectorizer for review comments
tfidf_vectorizer = TfidfVectorizer(
    min_df = 5,
    max_df = 0.90,
    stop_words = 'english',
    ngram_range = (1, 3)
)

# Transform cleaned review comments into a TF-IDF matrix
x_tfidf = tfidf_vectorizer.fit_transform(reviews_df['comments'])

# Extract feature names from the TF-IDF vocabulary
tfidf_feature_names = tfidf_vectorizer.get_feature_names_out()

# Convert TF-IDF matrix into a dataframe for readability
tfidf_df = pd.DataFrame(x_tfidf.toarray(), columns = tfidf_feature_names)

# Display TF-IDF matrix dimensions and sample records
print('TF-IDF matrix shape: ', x_tfidf.shape)
tfidf_df.head()

"""***`Observations:`***

- The TF IDF vectorizer transformed 5982 cleaned review comments into a matrix with 50472 weighted text features.

- The feature count is the same as the Bag of Words matrix because the same filtering rules and ngram range were applied.

- Unlike Bag of Words, TF IDF assigns higher importance to terms that are more meaningful within specific reviews and lower importance to very common terms.

- The matrix is highly sparse because each review contains only a small subset of the complete vocabulary.

- This representation is useful for topic modeling and text analysis because it captures the relative importance of words and phrases instead of relying only on raw word counts.

### Define functions to create plots
"""

# Define function to visualize top keywords for each topic
def plot_top_words(model, feature_names, n_top_words=10, title = None):

    # Get the number of topics from the fitted topic model
    n_topics = model.components_.shape[0]

    # Create subplot layout for topic keyword charts
    fig, axes = plt.subplots(2, (n_topics + 1) // 2, figsize=(15, 8), sharex=False)
    axes = axes.flatten()

    # Plot top weighted words for each topic
    for topic_idx, topic in enumerate(model.components_):
        top_idx = topic.argsort()[-n_top_words:][::-1]
        top_features = [feature_names[i] for i in top_idx]
        weights = topic[top_idx]
        axes[topic_idx].barh(top_features[::-1], weights[::-1])
        axes[topic_idx].set_title(f"Topic {topic_idx+1}")

    # Hide unused subplot areas
    for ax in axes[n_topics:]:
        ax.axis("off")

    # Add overall title and display plot
    fig.suptitle(title, fontsize=16)
    plt.tight_layout()
    plt.show()

# Define function to generate word clouds for topic keywords
def plot_word_clouds(model, feature_names, n_top_words=30, max_words=100, title = None):

    # Get the number of topics from the fitted topic model
    n_topics = model.components_.shape[0]

    # Create subplot layout for topic word clouds
    fig, axes = plt.subplots(2, (n_topics + 1) // 2, figsize=(15, 8))
    axes = axes.flatten()

    # Generate a word cloud for each topic
    for topic_idx, topic in enumerate(model.components_):
        top_idx = topic.argsort()[-n_top_words:]
        freqs = {feature_names[i]: float(topic[i]) for i in top_idx}
        wc = WordCloud(
            width=800,
            height=400,
            background_color='white',
            max_words=max_words
            ).generate_from_frequencies(freqs)
        axes[topic_idx].imshow(wc, interpolation="bilinear")
        axes[topic_idx].axis("off")
        axes[topic_idx].set_title(f"Topic {topic_idx+1}")

    # Hide unused subplot areas
    for ax in axes[n_topics:]:
        ax.axis("off")

    # Display topic word cloud plots
    plt.tight_layout()
    plt.show()

"""***`Observations:`***

- The plotting functions were created to make topic modeling results easier to interpret visually.

- plot_top_words() displays the most important keywords for each topic using horizontal bar charts, helping compare keyword weights across topics.

- plot_word_clouds() generates word clouds for each topic, where larger words represent stronger importance within that topic.

- Both functions automatically detect the number of topics from the fitted model, making them reusable for LDA, NMF, and LSA models.

- These visualizations help translate model outputs into readable topic themes, making it easier to understand what each topic represents in Airbnb guest reviews.

### ***Traditional Topic Modeling Approaches***

#### ***Latent Dirichlet Allocation (LDA)***
"""

# Set the number of topics for LDA topic modeling
n_topics = 5

# Initialize LDA model for identifying hidden review topics
lda_model = LatentDirichletAllocation(n_components = n_topics, random_state = 1)

# Fit LDA model using the Bag-of-Words matrix
lda_model.fit(x_bow)

# Extract feature names from the Bag-of-Words vectorizer
terms = bow_vectorizer.get_feature_names_out()

# Create dictionary to store top words for each LDA topic
lda_topics = {}

# Extract the top 10 keywords from each topic
for index, topic in enumerate(lda_model.components_):
  top_words = [terms[i] for i in topic.argsort()[-10:][::-1]]
  lda_topics[f'Topic {index + 1}'] = top_words

# Display the extracted LDA topics and their top keywords
print('LDA Topics:')
for topic, words in lda_topics.items():
  print(f'{topic}: {', '.join(words)}')

# Visualize the top keywords for each LDA topic
plot_top_words(lda_model, terms)

# Generate word clouds to visualize keywords in each LDA topic
plot_word_clouds(lda_model, terms)

"""***`Observations on LDA Topic Modeling:`***

- The LDA model was built using the Bag-of-Words matrix with 5 topics, and it successfully grouped review text into major recurring guest experience themes.

- Topic 1 and Topic 4 are strongly related to convenience and positive stay experience, with keywords such as easy, definitely, responsive, close, parking, and helpful, showing that guests frequently value accessibility and host support.

- Topic 2 mainly highlights group and family stays, with words like group, large, family, spacious, and plenty, suggesting that many reviews discuss suitability for larger guest groups.

- Topic 3 captures potential issue-related experiences, with keywords such as issue, night, door, bed, didn’t, and day, indicating that some reviews mention stay-related problems or discomfort.

- Overall, the LDA results show that Airbnb reviews are mainly centered around host responsiveness, location convenience, parking, spaciousness, restaurants, and guest issues, which can be useful for improving guest satisfaction and supporting price prediction features.
"""

# Create a copy of the reviews dataset to store LDA topic scores
lda_topics_df = reviews_df.copy()

# Generate topic probability distribution for each review
topic_distributions = lda_model.transform(x_bow)

# Add each topic score as a separate column
for i in range(n_topics):
  lda_topics_df[f'topic_{i + 1}'] = topic_distributions[:, i]

# Display reviews with assigned LDA topic scores
lda_topics_df.head()

# Save dataframe as CSV
lda_topics_df.to_csv('lda_topics_df.csv', index = False)

# Download the CSV file
from google.colab import files
files.download('lda_topics_df.csv')

"""***`Observations on LDA Topic Scores for Price Prediction:`***

- The LDA topic distribution converts each review into numerical topic scores, where each topic_1 to topic_5 column represents how strongly that review belongs to a specific guest-experience theme.

- Most records show one dominant topic score close to 1, while the remaining topic scores are very small, meaning each review is mainly associated with one major theme such as responsiveness, location convenience, group stay, issues, or nearby restaurants.

- These topic scores are useful because they transform unstructured review text into structured numerical features that can be merged with the Airbnb listings dataset.

- By incorporating these topic features into the neural network price prediction model, the model can learn whether certain review themes are associated with higher or lower listing prices.

- Overall, these LDA topic scores add guest-experience information to the price prediction pipeline, helping the model capture qualitative factors beyond standard listing features like bedrooms, location, ratings, and number of reviews.

#### ***Non-Negative Matrix Factorization***
"""

# Set the number of topics for NMF topic modeling
n_topics = 5

# Initialize NMF model for extracting review topics
nmf_model = NMF(n_components = n_topics, random_state = 1)

# Fit NMF model using the TF-IDF matrix
nmf_model.fit(x_tfidf)

# Extract feature names from the TF-IDF vectorizer
terms = tfidf_vectorizer.get_feature_names_out()

# Create dictionary to store top words for each NMF topic
nmf_topics = {}

# Extract the top 10 keywords from each topic
for index, topic in enumerate(nmf_model.components_):
  top_words = [terms[i] for i in topic.argsort()[-10:][::-1]]
  nmf_topics[f'Topic {index + 1}'] = top_words

# Display the extracted NMF topics and their top keywords
print('NMF Topics:')
for topic, words in nmf_topics.items():
  print(f'{topic}: {', '.join(words)}')

# Visualize the top keywords for each NMF topic
plot_top_words(nmf_model, terms)

# Generate word clouds to visualize keywords in each NMF topic
plot_word_clouds(nmf_model, terms)

"""***`Observations on NMF Topic Modeling:`***

- The NMF model identified five clear guest-experience themes from the Airbnb review text using TF-IDF features. Unlike LDA, the NMF topics appear more interpretable and business-friendly because each topic is centered around distinct review patterns.

- Topic 1 focuses on location convenience and accessibility, with keywords such as restaurant, walk, close, line, walkable, train, shop, and distance. This suggests many guests strongly discuss how easy it is to access restaurants, public transport, shops, and nearby attractions.

- Topic 2 captures property features and amenities, with terms such as view, building, staff, pool, hotel, lake, amenity, and rooftop. This topic may help identify listings where premium features, views, or building-level amenities influence guest perception and potentially price.

- Topic 3 represents negative or issue-related experiences, including words like night, issue, didn’t, unit, door, price, parking, check, and time. This topic is important because it may indicate problems related to check-in, parking, doors, pricing concerns, or stay interruptions.

- Topic 4 highlights large-group and family suitability, with keywords such as group, large, family, spacious, plenty, bathroom, garage, and bedroom. Listings with higher scores in this topic may be better suited for families or group travelers and could command different pricing behavior.

- Topic 5 reflects positive host and overall guest satisfaction, with words like definitely, beautiful, responsive, friendly, helpful, quiet, loved, and super. This topic captures strong positive guest sentiment and host responsiveness, which can be valuable predictive features for the Airbnb price prediction neural network.
"""

# Create a copy of the reviews dataset to store NMF topic scores
nmf_topics_df = reviews_df.copy()

# Generate topic weight distribution for each review
topic_distributions = nmf_model.transform(x_tfidf)

# Add each NMF topic score as a separate column
for i in range(n_topics):
  nmf_topics_df[f'topic_{i + 1}'] = topic_distributions[:, i]

# Display reviews with assigned NMF topic scores
nmf_topics_df.head()

# Save dataframe as CSV
nmf_topics_df.to_csv('nmf_topics_df.csv', index = False)

# Download the CSV file
from google.colab import files
files.download('nmf_topics_df.csv')

"""***`Observations on NMF Topic Scores:`***

- The NMF model assigns topic weight scores to each review, showing how strongly every review is associated with the topics.

- The scores are continuous values rather than fixed class labels, so a single review can contain signals from multiple themes at the same time.

- In the displayed records, Topic 5 has relatively higher weights for several reviews, suggesting that many sample reviews are strongly linked to positive guest experience themes such as responsiveness, friendliness, helpfulness, and satisfaction.

- Some reviews also show noticeable weights for Topic 1, which reflects location convenience, walkability, restaurants, and nearby access.

- Topic scores make the unstructured review text more measurable by converting guest comments into structured numerical features.

- The lower or zero values for some topics indicate that those themes are weakly represented or absent in the corresponding review.

- Overall, this step helps summarize each review based on dominant guest experience themes instead of relying only on raw text.

#### ***Latent Semantic Analysis (LSA)***
"""

# Set the number of topics for LSA topic modeling
n_topics = 5

# Initialize LSA model using Truncated SVD
lsa_model = TruncatedSVD(n_components = n_topics, random_state = 1)

# Fit LSA model using the Bag-of-Words matrix
lsa_model.fit(x_bow)

# Extract feature names from the Bag-of-Words vectorizer
terms = bow_vectorizer.get_feature_names_out()

# Create dictionary to store top words for each LSA topic
lsa_topics = {}

# Extract the top 10 keywords from each topic
for index, topic in enumerate(lsa_model.components_):
  top_words = [terms[i] for i in topic.argsort()[-10:][::-1]]
  lsa_topics[f'Topic {index + 1}'] = top_words

# Display the extracted LSA topics and their top keywords
print('LSA Topics:')
for topic, words in lsa_topics.items():
  print(f'{topic}: {', '.join(words)}')

# Visualize the top keywords for each LSA topic
plot_top_words(lsa_model, terms)

# Generate word clouds to visualize keywords in each LLSA topic
plot_word_clouds(lsa_model, terms)

"""***`Observations on LSA Topic Modeling:`***

- The LSA model identified 5 major hidden themes from guest reviews, capturing important patterns related to guest experience, location convenience, property quality, and stay suitability.

- Topic 1 focuses on general positive stay experience, with words like easy, responsive, definitely, close, parking, and quiet, showing that guests value convenience and host communication.

- Topic 2 highlights family/group suitability, with terms such as beautiful, family, group, spacious, large, and enjoyed, indicating that many reviews discuss comfort and space for larger stays.

- Topic 3 is strongly related to neighborhood accessibility and nearby attractions, with keywords like restaurant, shop, walkable, bar, walking distance, and coffee, showing the importance of location-based amenities.

- Topic 4 combines group stay features and potential practical concerns, including parking, unit, view, building, and issue, suggesting that both property size and minor stay-related problems appear in reviews.

- Topic 5 emphasizes property appeal and amenities, with words such as view, building, beautiful, amenity, lake, staff, hotel, and pool, indicating that visual appeal and facilities influence guest perception.
"""

# Create a copy of the reviews dataset to store LSA topic scores
lsa_topics_df = reviews_df.copy()

# Generate topic component scores for each review
topic_distributions = lsa_model.transform(x_bow)

# Add each LSA topic score as a separate column
for i in range(n_topics):
  lsa_topics_df[f'topic_{i + 1}'] = topic_distributions[:, i]

# Display reviews with assigned LSA topic scores
lsa_topics_df.head()

# Save dataframe as CSV
lsa_topics_df.to_csv('lsa_topics_df.csv', index = False)

# Download the CSV file
from google.colab import files
files.download('lsa_topics_df.csv')

"""***`Observations on LSA Topic Scores:`***

- The LSA topic scores convert each review into 5 numerical topic components, representing how strongly each review is associated with the hidden topics identified earlier.

- Unlike LDA or NMF, LSA scores can be positive or negative because Truncated SVD captures topic directions in a lower-dimensional semantic space.

- Topic 1 has consistently high positive scores across the displayed reviews, suggesting that the first LSA component captures the strongest common review pattern, such as general stay experience, host responsiveness, and convenience.

- Negative values in certain topic columns do not mean negative sentiment; they indicate that the review is positioned in the opposite direction of that topic component in the semantic space.

- Overall, these LSA topic score columns provide a compact numerical representation of review text, making unstructured guest feedback easier to analyze with structured listing-level data

#### Topic Distance
"""

# PCA library for reducing topic dimensions
from sklearn.decomposition import PCA

# Define function to visualize topic distances using PCA
def plot_topic_distances_pca(model, title = 'Topic Distance (PCA)'):

  # Extract topic-word matrix from the topic model
  top_word_matrix = model.components_

  # Reduce topic-word matrix into two principal components
  pca = PCA(n_components = 2)
  topic_pca = pca.fit_transform(top_word_matrix)

  # Create scatter plot of topics in 2D PCA space
  plt.figure(figsize = (8, 6))
  plt.scatter(topic_pca[:, 0], topic_pca[:, 1], c = 'blue', edgecolor = 'k', s = 100)

  # Add topic labels to each point
  for i in range(len(topic_pca)):
    plt.text(topic_pca[i, 0], topic_pca[i, 1], f'Topic {i + 1}', fontsize = 12)

  # Add chart title and axis labels
  plt.title(title)
  plt.xlabel('PCA Component 1')
  plt.ylabel('PCA Component 2')

  # Display PCA topic distance plot
  plt.show()

# Compare topic distances across all topic modeling methods
plot_topic_distances_pca(lda_model, title = 'LDA Topic Distance (PCA)')  # LDA
plot_topic_distances_pca(nmf_model, title = 'NMF Topic Distance (PCA)')  # NMF
plot_topic_distances_pca(lsa_model, title = 'LSA Topic Distance (PCA)')  # LSA

"""***`Observations:`***

- The LDA topic distance plot shows that the five topics are widely separated, especially Topic 4, indicating that LDA identified relatively distinct themes in the review text.

- In the NMF plot, Topics 3, 4, and 5 are positioned close to each other, suggesting some overlap among these topics, while Topics 1 and 2 are clearly separated and represent more unique themes.

- The LSA plot shows Topic 1 as strongly separated from the remaining topics, meaning it captures a dominant theme that is different from the others.

- Topics that are closer together in the PCA plots likely share similar keywords or review patterns, while topics farther apart represent more distinct guest experience themes.

- Overall, the topic distance visualizations help compare how well each method separates review themes, with LDA showing stronger topic separation, NMF showing moderate overlap, and LSA capturing one highly dominant topic with other topics clustered more closely.

### ***Modern Topic Modeling with BERTopic***
"""

# Install BERTopic library for transformer-based topic modeling
!pip install bertopic

# BERTopic library for transformer-based topic modeling
from bertopic import BERTopic

# Prepare review comments for BERTopic modeling
texts = reviews_df['comments'].tolist()

# Initialize BERTopic model
bertopic_model = BERTopic()

# Fit BERTopic model and assign topics to reviews
topics, probabilities = bertopic_model.fit_transform(texts)

# Retrieve BERTopic topic summary information
topic_info = bertopic_model.get_topic_info()

# Display topic IDs, counts, and representative keywords
print(topic_info)

# Visualize the top words and their importance for each BERTopic topic
bertopic_model.visualize_barchart()

"""***`Observations:`***

- The topic modeling results group Airbnb reviews into meaningful themes such as cleanliness issues (Topic 1), positive guest experiences (Topics 2 & 5), premium amenities and views (Topic 3), accessibility and noise factors (Topic 4), and location-specific context (Topic 6). This indicates that customer feedback captures multiple dimensions of the stay experience beyond basic listing features.

- The topics reflect a clear distinction between positive and negative sentiments. Words like “beautiful”, “best”, and “experience” indicate high customer satisfaction, while terms like “dirty”, “refund”, and “noise” highlight service or quality issues. This suggests that reviews act as strong signals of perceived value, which can influence listing prices.

- Topic-based features add value to the model by incorporating unstructured review data into structured prediction. While traditional variables explain physical aspects of listings, topic scores capture customer perception and experience quality. This helps differentiate listings with similar attributes but different user experiences, improving the model’s ability to predict price more accurately.
"""

# Visualize the hierarchical relationship between discovered BERTopic topics
bertopic_model.visualize_hierarchy()

"""***`Observations:`***

- The hierarchical clustering plot shows how the BERTopic-generated topics are grouped based on semantic similarity in the Airbnb review data.

- Most topics are connected within a moderate distance range, indicating that many review themes are related to each other and share overlapping words or meanings.

- Several topics appear to form smaller clusters around common Airbnb experience themes such as restaurants, walking distance, train access, group/family stays, parking, building, pool, rooftop, and host responsiveness.

- Topics related to location and nearby attractions, such as restaurants, walking distance, train, Logan Square, Chinatown, and shopping areas, appear frequently, showing that location convenience is a major theme in guest reviews.

- Topics related to property quality and amenities, such as view, pool, rooftop, building, balcony, and spacious rooms, also form meaningful groups, suggesting that guests often describe physical features of the listing.

- Some topics connected to guest experience and host interaction, such as responsive, helpful, excellent, thanks, and easy, indicate that service quality and communication are important parts of review content.

- A few topics merge only at a larger distance, meaning they are more distinct from the rest. For example, topics around “view/excellent” and “experience/thanks” appear somewhat separate, suggesting more specific review themes.

- Overall, the clustering confirms that BERTopic successfully identified interpretable review themes and grouped similar topics together, helping summarize large volumes of guest feedback into meaningful categories.


"""

# Visualize topic similarity across BERTopic topics using a heatmap
bertopic_model.visualize_heatmap()

"""***`Observations:`***

- The similarity matrix shows the semantic similarity between different BERTopic-generated review topics. Darker blue cells indicate higher similarity, while lighter cells indicate lower similarity.

- The diagonal line has the highest similarity scores because each topic is being compared with itself, which naturally gives the strongest match.

- Most of the topics show medium to high similarity with each other, suggesting that Airbnb reviews share overlapping themes such as location, host responsiveness, restaurants, amenities, family stays, parking, and property experience.

- Topics such as restaurant, easy, close, walking distance, train, and Chinatown appear strongly related, indicating that many guest reviews focus on location convenience and nearby attractions.

- Topics related to view, pool, rooftop, building, balcony, and hotel/staff also show similarity, suggesting that guests frequently discuss property features and amenities together.

- Some lighter vertical and horizontal bands are visible, meaning a few topics are less similar to the majority of other topics. These topics may represent more unique or specialized guest experiences.

- The matrix supports the hierarchical clustering result because similar groups of topics appear close together and show stronger similarity patterns.

- Overall, the similarity matrix confirms that the topic model captured meaningful and related review themes, while still identifying a few distinct topics within the Airbnb guest feedback.
"""

# Visualize inter-topic distances for BERTopic topics
bertopic_model.visualize_topics()

"""***`Observations:`***

- The intertopic distance map shows how the generated topics are positioned based on their semantic similarity. Topics that are closer together represent similar review themes, while topics that are farther apart represent more distinct themes.

- Several topics are clustered near the center of the map, indicating that many Airbnb review topics share overlapping words and meanings, such as guest experience, responsiveness, convenience, and property-related feedback.

- A few topic groups are clearly separated from the central cluster, especially those positioned toward the top, right, and bottom-left areas. This suggests that the model has identified some distinct review themes rather than grouping all reviews into one broad topic.

- The large bubble on the lower-right side indicates a topic with a relatively higher number of reviews compared to smaller bubbles. This means that this topic represents a more dominant theme in the review dataset.

- Smaller bubbles represent topics with fewer reviews, suggesting that some themes are more specific or less frequently discussed by guests.

- Topics that appear close to each other can be interpreted as related sub-themes. For example, topics about location, restaurants, walking distance, and nearby attractions may appear closer because they describe similar guest concerns.

- Topics that are far apart may represent different guest experience dimensions, such as property amenities, neighborhood convenience, host responsiveness, or specific issues mentioned in reviews.

- Overall, the intertopic distance map confirms that the topic model captured both common review patterns and distinct guest experience themes, making it useful for understanding the major discussion areas in Airbnb guest feedback.

## ***Price Prediction Model***

### Initial exploration of Listings Dataset
"""

# Display the first 5 rows of the listings dataframe to preview the data
listings_df.head()

# Display a concise summary of the dataframe including columns, data types, and non-null counts
listings_df.info()

"""***`Observations:`***

- The listings dataset contains 8,660 Airbnb records and 79 columns, including host information, property details, availability, reviews, location, amenities, and pricing-related variables.

- The target variable for the price prediction model is price, but it has missing values. Out of 8,660 records, only 7,825 listings have non-null price values, so missing price records need to be removed before model training.

- The dataset includes strong structured predictors such as accommodates, bathrooms, bedrooms, beds, minimum nights, availability, number of reviews, review scores, host acceptance rate, host listing counts, latitude, and longitude, which can help explain listing price variation.

- Several columns contain missing values, especially neighborhood_overview, host_about, neighbourhood, review score columns, bathrooms, beds, bedrooms, and reviews_per_month. These missing values should be handled using appropriate imputation or column removal depending on their importance.

- Some columns such as listing_url, picture_url, host_url, host_thumbnail_url, host_picture_url, scrape_id, last_scraped, calendar_last_scraped, and source are mostly identifiers or metadata and may not directly improve price prediction.

- The dataset includes both numerical and categorical features, so preprocessing is required before modeling. Numerical columns need scaling, while categorical variables such as room_type, property_type, neighbourhood_cleansed, instant_bookable, and host_is_superhost need encoding.

- The review score variables such as rating, cleanliness, communication, location, and value can provide useful information about guest satisfaction, which may have a relationship with price.

- The location-based variables such as latitude, longitude, and neighbourhood information are important because Airbnb prices are highly influenced by area, accessibility, and nearby attractions.

- The dataset also includes availability and occupancy-related columns, such as availability_30, availability_60, availability_90, availability_365, estimated_occupancy_l365d, and estimated_revenue_l365d. These can help capture demand and booking behavior.

- Since the model uses Airbnb listing data with many features, feature selection and regularization are important to reduce noise, avoid overfitting, and improve generalization on unseen data.
"""

# Get the dimensions of the dataframe (rows, columns)
listings_df.shape

"""***`Observations:`***

- The listings dataset contains 8660 rows and 79 columns indicating a wide dataset with many features.

- Each row represents a unique listing while the large number of columns suggests detailed attributes such as pricing location amenities and host information.

- This dataset is suitable for in depth analysis and feature rich modeling tasks like pricing prediction.

### Filtering data where the host location is Chicago, IL
"""

# Get all unique values from the 'host_location' column
listings_df['host_location'].unique()

# Count the number of unique host locations in the dataset
listings_df['host_location'].nunique()

# Filter the dataframe to include only listings where host location is Chicago, IL
listings_df = listings_df[listings_df['host_location'] == 'Chicago, IL']

# Get the dimensions of the filtered dataframe (rows, columns)
listings_df.shape

"""***`Observations:`***

- The dataset contains 212 unique host locations indicating wide geographic diversity across listings.

- Filtering for Chicago, IL narrows the dataset to 5439 listings allowing focused analysis on a single market.

- This filtering helps remove location based variability making insights more consistent especially for tasks like price prediction.

### Merging Listings Data with Sentiment, Zero-Shot Classification, and LDA Topic Score Datasets
"""

# Merge listings data with Hugging Face sentiment results using listing IDs
listings_df = pd.merge(
    listings_df,
    huggingface_sentiment_reviews_df,
    left_on = 'id',
    right_on = 'listing_id',
    how = 'inner'
)

# Merge listings data with zero-shot review category scores
listings_df = pd.merge(
    listings_df,
    zero_shot_df,
    left_on = 'id',
    right_on = 'review_id',
    how = 'inner'
)

# Merge listings data with LDA topic scores using listing IDs
listings_df = pd.merge(
    listings_df,
    lda_topics_df,
    left_on = 'id',
    right_on = 'listing_id',
    how = 'inner'
)

"""***`Observations:`***

- The listings dataset is merged with Hugging Face sentiment results using id from listings_df and listing_id from huggingface_sentiment_reviews_df.

- This merge adds review sentiment-related features such as the sentiment label and sentiment confidence score to the listings dataset.

- The dataset is then merged with zero-shot classification scores using id from listings_df and review_id from zero_shot_df.

- This step adds guest experience category scores such as Location, Amenities, Service, Cleanliness, and Safety.

- Finally, the listings dataset is merged with LDA topic scores using id from listings_df and listing_id from lda_topics_df.

- This adds topic distribution features that represent the main themes discussed in guest reviews.

- Since all three merges use how='inner', only listings that have matching records in all review-based datasets are retained.

- The final dataset combines structured listing-level information with NLP-derived features from guest reviews, making it richer for analysis and model building.
"""

# Display merged listings dataset with review-based features
listings_df.head()

# Check the size of the merged listings dataset
listings_df.shape

"""***`Observations:`***

- The listings dataset was merged with three NLP-based review datasets: Hugging Face sentiment analysis, zero-shot classification scores, and LDA topic scores.

- The merge was performed using listing-level identifiers, where id from the listings dataset was matched with listing_id or review_id from the NLP output datasets.

- An inner join was used in all three merge operations, meaning only listings that had matching records in all NLP-based datasets were retained.

- After merging, the final dataset contains 4,164 rows and 96 columns.

- The increase in the number of columns shows that additional review-based features were added to the original listings dataset, including sentiment scores, sentiment labels, guest experience category scores, and topic scores.

- Since the final merged dataset has 4,164 records, some listings may have been removed during merging if they did not have corresponding review, sentiment, zero-shot, or topic modeling information.

- The merged dataset now combines both structured listing information and unstructured review-derived insights, making it richer for further analysis and modeling.

- This merged dataset provides a more complete view of each listing by including property details along with guest feedback patterns extracted from review text.

### Selecting relevant features from the dataset
"""

# Select relevant features for analysis and modeling
selected_columns = [
    # Property Characteristics
    'room_type',
    'accommodates',
    'bedrooms',
    'bathrooms',

    # Location
    'latitude',
    'longitude',

    # Host Quality Signals
    'host_is_superhost',
    'host_acceptance_rate',

    # Review & Rating Signals
    'review_scores_rating',
    'number_of_reviews',

    # Availability & Booking
    'availability_365',
    'instant_bookable',

    # Hugging Face Sentiment Scores
    'huggingface_score',

    # Zero-Shot Classification Categories
    'Location',
    'Amenities',
    'Service',
    'Cleanliness',
    'Safety',

    # LDA Topic Scores
    'topic_1',
    'topic_2',
    'topic_3',
    'topic_4',
    'topic_5',

     # Target
    'price'
]

listings_df = listings_df[selected_columns]

"""***`Observations:`***

- A focused set of relevant columns was selected from the merged dataset for further analysis and model building.

- The selected features include key property characteristics such as room_type, accommodates, bedrooms, and bathrooms, which help describe the physical capacity and type of each Airbnb listing.

- Location-based variables, latitude and longitude, were retained to capture the geographical influence on listing prices.

- Host-related quality indicators such as host_is_superhost and host_acceptance_rate were included to represent host credibility and booking responsiveness.

- Review and rating signals, including review_scores_rating and number_of_reviews, were selected because they reflect guest satisfaction and listing popularity.

- Availability and booking-related features such as availability_365 and instant_bookable were retained to capture listing accessibility and booking convenience.

- NLP-derived features were included from Hugging Face sentiment analysis, zero-shot classification, and LDA topic modeling to capture guest feedback, sentiment, experience categories, and hidden review themes.

- The final selected dataset keeps price as the target variable, while all other selected columns act as input features for analysis and prediction.

### Overview of the Listings Dataset
"""

# Display the first five records to verify selected listing and review features
listings_df.head()

# Display the last five records of the listings dataset
listings_df.tail()

# Display a concise summary of the dataframe including columns, data types, and non-null counts
listings_df.info()

# Get the dimensions of the dataframe (rows, columns)
listings_df.shape

"""***`Observations:`***

- The final selected dataset contains 4,164 rows and 24 columns, which shows that the dataset has been reduced to only the most relevant variables needed for analysis and modeling.

- The selected features include a good mix of property characteristics, location details, host-related features, review signals, booking availability, sentiment scores, zero-shot classification scores, LDA topic scores, and price.

- Most columns have complete data with 4,164 non-null values, especially location variables, review-related variables, sentiment scores, zero-shot category scores, and LDA topic score columns.

- Some important columns contain missing values, such as bedrooms, bathrooms, host_is_superhost, host_acceptance_rate, and price, which need to be handled before building the model.

- The target variable price has only 3,840 non-null values, meaning some listings do not have price information and may need to be removed or cleaned before model training.

- The dataset contains 16 float columns, 3 integer columns, and 5 object columns, so categorical variables like room_type, host_is_superhost, host_acceptance_rate, instant_bookable, and price may require data type conversion or encoding.

- The inclusion of huggingface_score, zero-shot category scores, and LDA topic scores adds useful text-based guest review information to the structured listing data.

- Overall, the dataset is now well-structured for the next preprocessing steps, including missing value treatment, data type conversion, categorical encoding, scaling, and model preparation.

### Data Cleaning

#### Initial Sanity Checks of the Dataset
"""

# Count the number of duplicate rows in the dataframe
listings_df.duplicated().sum()

"""***`Observations:`***

- The dataset has 0 duplicate rows, which means there are no completely repeated records in the selected listings dataset.

- This indicates that each row represents a unique listing-review merged record after combining listings, sentiment, zero-shot, and LDA topic features.
Since no duplicates are present, there is no need to remove duplicate records at this stage.

- This improves data quality and reduces the risk of biasing the price prediction model with repeated observations.

- The dataset is ready to move forward with missing value handling, data type conversion, outlier treatment, and feature preprocessing.
"""

# Count the number of missing (null) values in each column
listings_df.isnull().sum()

"""***`Observations:`***

- The dataset has no duplicate rows, indicating that each record is unique and there is no repeated listing-review combination after merging.

- Missing values are present in a few important columns, mainly bathrooms (314 missing), price (324 missing), host_is_superhost (157 missing), host_acceptance_rate (106 missing), and bedrooms (99 missing).

- Since price is the target variable, rows with missing price values should be removed before model training because they cannot contribute to supervised price prediction.

- Missing values in bedrooms, bathrooms, host_is_superhost, and host_acceptance_rate need to be handled carefully using imputation techniques, as these variables can influence listing price.

- NLP-derived features such as Hugging Face sentiment score, zero-shot category scores, and LDA topic scores have no missing values, which means they are ready to be used directly after feature scaling if required.

#### Feature Engineering
"""

# Convert 'price' column from string to numeric by removing currency symbols and commas
listings_df['price'] = listings_df['price'].replace(r'[\$,]', '', regex = True).astype(float)

# Convert 'host_acceptance_rate' from percentage string to numeric by removing '%' symbol
listings_df['host_acceptance_rate'] = listings_df['host_acceptance_rate'].replace(r'[\%]', '', regex = True).astype(float)

"""***`Observations:`***

- The price column was originally stored as an object/string because it contained currency symbols such as $ and commas, so it was converted into a numeric float format for modeling.

- The host_acceptance_rate column was also stored as an object because it contained the % symbol, which was removed before converting the column into a numeric format.

- Converting these columns is an important feature engineering step because machine learning models and neural networks require numerical inputs for calculations.

- After conversion, both price and host_acceptance_rate can be used properly in statistical analysis, correlation analysis, scaling, and model training.

- This step improves data consistency and prepares the dataset for further preprocessing tasks such as missing value treatment, outlier handling, encoding, and feature scaling.

#### Handling Missing Values in the Dataset
"""

# Drop rows with missing values in 'price' and check remaining null values
listings_df.dropna(subset = 'price', inplace = True)
listings_df.isnull().sum()

"""***`Observations:`***

- The price column was converted from an object/string format into a numeric format by removing the dollar sign and comma symbols, making it suitable to use as the target variable for price prediction.

- The host_acceptance_rate column was also converted from a percentage string into a numeric feature by removing the % symbol, allowing it to be used properly in statistical analysis and machine learning models.

- Rows with missing values in the target variable price were removed because records without price values cannot contribute to supervised price prediction.

- After dropping missing price records, the target column has no null values remaining, which ensures the model has a valid output value for every observation.

- A few missing values still remain in predictor columns, mainly bedrooms, host_is_superhost, and host_acceptance_rate, so these features need to be handled through imputation or suitable preprocessing before model training.
"""

# Fill missing 'bedrooms' values with median and check remaining null values
listings_df['bedrooms'].fillna(listings_df['bedrooms'].median(), inplace = True)
listings_df.isnull().sum()

"""***`Observations:`***

- Missing values in the bedrooms column were filled using the median, which is suitable because bedrooms is a numerical feature and the median is less affected by extreme values.

- After imputation, the bedrooms column has no missing values, so it is now ready to be used for model training.

- The remaining missing values are only in host_is_superhost and host_acceptance_rate, with 151 and 74 null values respectively.

- Since host_is_superhost is categorical and host_acceptance_rate is numerical, they should be handled separately using mode imputation for the categorical feature and median or grouped median imputation for the numerical feature.

- All other selected variables, including review scores, sentiment scores, zero-shot category scores, topic scores, and the target variable price, are complete with no missing values.
"""

# Fill missing 'host_is_superhost' values with mode and check remaining null values
listings_df['host_is_superhost'].fillna(listings_df['host_is_superhost'].mode()[0], inplace = True)
listings_df.isnull().sum()

"""***`Observations:`***

- Missing values in the host_is_superhost column were filled using the mode, which is appropriate because this is a categorical variable with values such as t and f.

- After mode imputation, host_is_superhost has no missing values, meaning the superhost status feature is now complete for all records.

- Using the most frequent category helps preserve the dominant pattern in the dataset without removing additional rows.

- The only remaining column with missing values is host_acceptance_rate, which still has 74 null records.

- All other selected numerical, categorical, sentiment, zero-shot classification, LDA topic, and target columns are now complete and ready for further preprocessing.


"""

# Fill missing host acceptance rates using median values by superhost status and check remaining missing values in each column
listings_df['host_acceptance_rate'] = listings_df.groupby('host_is_superhost')['host_acceptance_rate'].transform(lambda x: x.fillna(x.median()))
listings_df.isnull().sum()

"""***`Observations:`***

- Missing values in host_acceptance_rate were handled using median imputation grouped by host_is_superhost, which is a more meaningful approach than using one overall median.

- This method preserves differences between superhosts and non-superhosts because acceptance rate behavior may vary by host category.

- After this step, host_acceptance_rate has no missing values, making the feature complete for modeling.

- The dataset now has no null values across all selected columns, including property details, host signals, review scores, sentiment scores, zero-shot categories, LDA topic scores, and price.

- The cleaned dataset is now ready for encoding categorical variables, scaling numerical features, checking outliers, and preparing train-test splits for the price prediction model.
"""

# Shape of the DataFrame (rows, columns) after handling missing values and duplicated records
listings_df.shape

"""***`Observations:`***

- No duplicate records were found in the dataset, indicating that each row represents a unique listing-review record and no duplicate removal was required.

- The dataset initially contained 4,164 rows and 24 selected columns, including property details, host information, review scores, NLP-based sentiment features, zero-shot category scores, LDA topic scores, and the target variable price.

- Missing values were identified in important columns such as bedrooms, bathrooms, host_is_superhost, host_acceptance_rate, and price, with price having 324 missing values, which needed to be removed because it is the target variable for prediction.

- After dropping rows with missing price, the dataset size reduced to 3,840 rows, and the remaining missing values were handled using suitable imputation methods: median for bedrooms, mode for host_is_superhost, and group-wise median for host_acceptance_rate based on superhost status.

- After completing the missing value treatment, the final cleaned dataset contains 3,840 rows and 24 columns with no missing values, making it ready for exploratory data analysis and machine learning model preparation.

### Exploratory Data Analysis (EDA)
"""

# Generate summary statistics for numerical columns and transpose for better readability
listings_df.describe(include = 'number').T

"""***`Observations:`***

- The cleaned dataset contains 3840 records, and all numerical columns now have complete values, confirming that the data is ready for EDA and model preparation.

- The listings generally accommodate around 5 guests on average, with a median of 4 guests, while most properties have 1 to 3 bedrooms and 1 to 2 bathrooms, showing that the dataset mainly consists of small to medium sized Airbnb properties.

- The average price is around 192, but the maximum price reaches 4500, indicating a highly right skewed price distribution with possible premium or luxury listing outliers.

- Host and review quality indicators are generally strong, with an average host acceptance rate of 93 percent, median review rating of 4.88, and median 48 reviews, suggesting that many listings are active, well rated, and have reasonable booking engagement.

- NLP based features show strong guest sentiment and review theme relevance, with high average Hugging Face sentiment score, high zero shot scores for Location, Amenities, Cleanliness, Service, and Safety, and LDA topic scores showing that Topic 4 is the most dominant theme on average, followed by Topic 1, while Topics 2, 3, and 5 are less dominant across reviews.
"""

# Generate summary statistics for categorical (object) columns and transpose for better readability
listings_df.describe(include = 'object').T

"""***`Observations:`***

- The cleaned dataset contains 3,840 records across the selected categorical columns, indicating that all remaining categorical fields have complete values after data cleaning.

- The room_type column has 4 unique categories, with Entire home or apartment being the most frequent category, appearing 3,111 times. This shows that most listings in the dataset are full-property rentals.

- The host_is_superhost column has 2 categories, with t appearing 2,487 times, indicating that a large portion of the listings are managed by superhosts.

- The instant_bookable column also has 2 categories, with f appearing 2,721 times, showing that most listings are not instantly bookable and may require host approval before confirmation.

- Overall, the categorical summary suggests that the dataset is dominated by entire-home listings, many listings are associated with superhosts, and instant booking is less common among the available records.
"""

# Identify numeric columns (excluding latitude and longitude) and categorical columns
Numeric_Columns = listings_df.select_dtypes(include = 'number').columns.drop(['latitude', 'longitude'])
Categorical_Columns = listings_df.select_dtypes(include = 'object').columns

"""***`Observations:`***

- The dataset columns were separated into numeric and categorical groups to make the EDA and preprocessing workflow more organized.

- Latitude and longitude were excluded from the numeric feature list because they represent geographic coordinates rather than regular numerical variables for distribution-based analysis.

- The numeric columns mainly include property capacity, host behavior, review scores, sentiment scores, zero-shot category scores, topic scores, availability, and price.

- The categorical columns include object-type variables such as room_type, host_is_superhost, and instant_bookable, which need encoding before machine learning modeling.

- This separation helps apply the correct analysis and preprocessing methods, such as histograms and boxplots for numeric columns, and countplots or encoding techniques for categorical columns.

#### Univariate Analysis
"""

# Create histograms to examine numeric feature distributions
plt.figure(figsize = (15, 20))

# Plot distribution and KDE curve for each numeric column
for i, col in enumerate(Numeric_Columns):
  plt.subplot(7, 3, i + 1)
  sns.histplot(data = listings_df, x = col, kde = True, color = 'Green')
  plt.title(f'Distribution of {col}')
  plt.xlabel(col)
  plt.ylabel('Frequency')

# Adjust layout and display all distribution plots
plt.tight_layout()
plt.show()

"""***`Observations:`***

- Most listings are designed for small to medium groups, as accommodates, bedrooms, and bathrooms are concentrated at lower values, while a few listings support much larger groups, indicating right-skewed distributions.

- host_acceptance_rate, review_scores_rating, and huggingface_score are highly concentrated near the upper end, showing that most hosts have high acceptance rates, strong guest ratings, and strongly positive review sentiment.

- Review-related zero-shot features such as Location, Amenities, Service, Cleanliness, and Safety also show scores close to 1 for most records, suggesting that guest reviews frequently contain positive or strong signals related to these experience categories.

- number_of_reviews is highly right-skewed, meaning most listings have a moderate number of reviews, while a small number of popular listings have very high review counts.

- availability_365 is widely distributed, but many listings show high availability, suggesting that several properties are available for a large portion of the year.

- LDA topic scores show uneven distributions across topics. topic_4 appears more dominant across many listings, while topic_2, topic_3, and topic_5 are mostly concentrated near zero, meaning those themes are less strongly represented in most records.

- The price distribution is strongly right-skewed, with most listings concentrated in lower price ranges and a small number of listings having very high prices, indicating the presence of outliers.

- Overall, several numeric variables are skewed and may require transformation, scaling, or outlier handling before modeling, especially price, number_of_reviews, availability_365, and selected topic score features.
"""

# Create boxplots to detect outliers in numeric features
plt.figure(figsize = (15, 20))

# Plot boxplot for each numeric column
for i, col in enumerate(Numeric_Columns):
  plt.subplot(7, 3, i + 1)
  sns.boxplot(data = listings_df, x = col)
  plt.title(f'Boxplot of {col}')

# Adjust layout and display all boxplots
plt.tight_layout()
plt.show()

"""***`Observations:`***

- The histogram and boxplot show that most listings are designed for smaller groups, with accommodates, bedrooms, and bathrooms concentrated around lower values, while a few large properties create right-skewed distributions and visible outliers.

- Host acceptance rate, review scores rating, Hugging Face sentiment score, and zero-shot category scores are highly concentrated near the upper end, indicating that most listings have strong host responsiveness, high guest ratings, positive review sentiment, and favorable review themes.

- Number of reviews is strongly right-skewed, where most listings have relatively fewer reviews, but a small number of highly reviewed listings appear as extreme outliers.

- Availability 365 is more widely distributed, suggesting that listings have different booking patterns, with some properties rarely available and others available for most of the year.

- LDA topic scores show that topic 4 and topic 1 are more dominant across many reviews, while topic 2, topic 3, and topic 5 are concentrated near lower values for most records, suggesting that only some listings strongly align with those specific review themes.

- Price is heavily right-skewed, with most listings concentrated in the lower price range and a few very high-priced listings creating strong outliers, which may need transformation or outlier treatment before modeling.
"""

# Create countplots to examine categorical feature distributions
plt.figure(figsize = (15, 10))

# Plot frequency count for each categorical column
for i, col in enumerate(Categorical_Columns):
  plt.subplot(2, 2, i+1)
  sns.countplot(data = listings_df, x = col, hue = col)
  plt.title(f'Countplot of {col}')
  plt.xticks(rotation = 90)

# Adjust layout and display all categorical plots
plt.tight_layout()
plt.show()

"""***`Observations:`***

- The room_type distribution is highly dominated by Entire home/apt, with more than 3000 listings, showing that most Airbnb properties in the dataset are full-unit rentals rather than shared or private room options.

- Private room appears as the second most common category, but its count is much lower than Entire home/apt, indicating that shared-space accommodation options are relatively limited in this dataset.

- Shared room and Hotel room have very low representation, suggesting that these categories may have minimal influence on the overall price prediction model compared to the dominant room types.

- The host_is_superhost countplot shows that superhosts marked as t are more frequent than non-superhosts, indicating that a large portion of listings are managed by highly rated or experienced hosts.

- The instant_bookable distribution shows that most listings are marked as f, meaning many hosts do not allow instant booking and likely prefer manual approval before confirming reservations.

- Overall, the categorical features are imbalanced, especially room_type and instant_bookable, so proper encoding is important before modeling to ensure the neural network can learn from these categorical patterns effectively.

#### Bivariate Analysis
"""

# Create pairwise plots to examine relationships among numeric features
sns.pairplot(data = listings_df[Numeric_Columns], diag_kind = 'kde')

# Display pairwise scatter plots and feature distributions
plt.show()

"""***`Observations:`***

- The pairplot shows that price is highly right-skewed, where most listings are concentrated at lower price levels, while a small number of listings have very high prices. This indicates the presence of strong price outliers.

- Accommodates, bedrooms, and bathrooms show a positive relationship with price. Listings that can host more guests or have more rooms generally tend to have higher prices, although the relationship is not perfectly linear.

- Accommodates, bedrooms, and bathrooms are strongly related to each other, which is expected because larger properties usually support more guests and have more bedrooms and bathrooms.

- Review scores, Hugging Face sentiment score, and zero-shot category scores are highly concentrated near the upper range, showing that most listings have very positive reviews and strong guest experience signals.

- The zero-shot features such as Location, Amenities, Service, Cleanliness, and Safety show similar high-score patterns, suggesting that guest reviews generally discuss these aspects positively across most listings.

- Number of reviews is right-skewed, meaning most listings have relatively fewer reviews, while a small number of listings have very high review counts. These highly reviewed listings may represent popular or long-running properties.

- Availability_365 is widely spread, indicating that some listings are available for only a few days, while others are available for most of the year. This could reflect differences between part-time and full-time Airbnb hosts.

- The LDA topic score features show different distribution patterns. Topic 4 appears more evenly spread, while topics such as topic 2, topic 3, and topic 5 are concentrated near lower values, meaning only certain reviews strongly belong to those specific topics.

- Several scatterplots involving price show dense clustering at lower price values with a few extreme high-price observations, confirming that price may need transformation or outlier treatment before modeling.

- Overall, the pairplot suggests that property size variables, availability, review volume, sentiment signals, guest experience scores, and topic scores may all provide useful information for predicting Airbnb listing prices.
"""

# Select numeric features excluding the target variable
num_cols = Numeric_Columns.drop('price')

# Create scatterplots to examine relationships between numeric features and price
plt.figure(figsize = (15, 20))

# Plot each numeric feature against Airbnb listing price
for i, col in enumerate(num_cols):
  plt.subplot(7, 3, i+1)
  sns.scatterplot(data = listings_df, x = col, y = 'price')
  plt.title(f'Scatterplot of {col}')

# Adjust layout and display all scatterplots
plt.tight_layout()
plt.show()

"""***`Observations:`***

- The scatterplots show that price generally increases with property size-related features such as accommodates, bedrooms, and bathrooms. Listings that can host more guests or have more rooms tend to show higher prices, although there is still wide variation.

- The price distribution is highly right-skewed, with most listings concentrated at lower price levels and a few listings having very high prices. These extreme high-price listings may behave like outliers and can strongly influence model training.

- host_acceptance_rate is heavily concentrated near high values, especially close to 100. This suggests that most hosts have high acceptance rates, but the relationship with price does not appear strongly linear.

- review_scores_rating, huggingface_score, and the zero-shot category scores such as Location, Amenities, Service, Cleanliness, and Safety are mostly concentrated near high values. This indicates that many listings have positive guest feedback, but these variables may have limited spread.

- number_of_reviews is right-skewed, where most listings have fewer reviews, while a small number of listings have very high review counts. Higher review count does not show a clear direct relationship with higher price.

- availability_365 is spread across the full range, indicating that some listings are rarely available while others are available for most of the year. However, the relationship between availability and price appears weak and scattered.

- The LDA topic scores show mixed patterns. topic_4 appears more evenly distributed across different score levels, while topic_2, topic_3, and topic_5 are mostly concentrated near lower values. This suggests that some topics are dominant in only a subset of reviews.

- Overall, the scatterplots suggest that property capacity features are likely to be more directly useful for predicting price, while review-based sentiment, zero-shot scores, and topic scores may provide additional supporting information about guest experience and listing quality.
"""

# Create boxplots to compare price distribution across categorical features
plt.figure(figsize = (15, 10))

# Plot price distribution for each categorical column
for i, col in enumerate(Categorical_Columns):
  plt.subplot(2, 2, i+1)
  sns.boxplot(data = listings_df, x =col, y = 'price', hue = col, showfliers = False)
  plt.title(f'Boxplot of {col}')
  plt.xticks(rotation = 90)

# Adjust layout and display all categorical price comparison plots
plt.tight_layout()
plt.show()

"""***`Observations:`***

- Room type shows a clear difference in price distribution. Entire home/apt has the highest overall price range and a much wider spread compared to private room and shared room.

- Hotel room has a higher median price than private room and shared room, but the number of hotel room listings appears much smaller, so the pattern should be interpreted carefully.

- Private rooms are generally priced lower, with most prices concentrated in a relatively smaller range. This suggests that private rooms are more budget-oriented compared to entire home/apt listings.

- Shared rooms have the lowest price range, showing that they are the most economical accommodation type in the dataset.

- Superhost listings show a slightly higher median price than non-superhost listings. This indicates that hosts with superhost status may be able to charge higher prices due to stronger reputation and better guest trust.

- Superhost listings also show a wider price range, meaning superhosts operate across both moderate and higher-priced listings.

- Instant bookable listings have a slightly higher median price than non-instant bookable listings. This suggests that convenience in booking may be associated with higher listing prices.

- Both instant bookable and non-instant bookable listings show wide variation in price, so instant booking alone may not strongly determine price, but it can still be a useful supporting feature for the prediction model.

#### Correlation Matrix
"""

# Create correlation heatmap to analyze relationships among numeric features
plt.figure(figsize = (20, 14))

# Plot correlation matrix with annotated correlation values
sns.heatmap(listings_df[Numeric_Columns].corr(), annot = True, vmin = '-1', vmax = '1', cbar = True, cmap = 'YlGnBu', fmt = '.2f')

# Display correlation heatmap
plt.show()

"""***`Observations:`***

- Price shows the strongest positive relationship with property capacity features.
The highest correlations with price are seen for bathrooms at 0.63, accommodates at 0.61, and bedrooms at 0.59. This indicates that larger listings with more space and facilities generally tend to have higher prices.

- Accommodates, bedrooms, and bathrooms are strongly related to each other.
Accommodates and bedrooms have a correlation of 0.84, bedrooms and bathrooms have 0.68, and accommodates and bathrooms have 0.66. This suggests that these variables capture similar property-size information and may create some multicollinearity in the model.

- Review-based experience scores are highly correlated with each other.
Location, Amenities, Service, Cleanliness, and Safety show strong positive correlations among themselves. For example, Service and Safety have a correlation of 0.86, Amenities and Cleanliness have 0.79, and Location and Amenities have 0.75. This indicates that listings with strong guest experience in one category often perform well in other categories too.

- Review experience scores have weak direct correlation with price.
Location, Amenities, Service, Cleanliness, and Safety have very low correlations with price, mostly between 0.04 and 0.06. This suggests that these text-derived guest experience features may not have a strong linear relationship with price, but they may still help neural networks capture nonlinear patterns.

- Topic 2 has a moderate positive relationship with price.
Among the topic modeling features, topic_2 has the highest correlation with price at 0.30. This suggests that reviews associated with topic_2 may contain themes that are more common in higher-priced listings.

- Other topic features show weak or negative relationships with price.
topic_1 has a negative correlation with price at minus 0.09, topic_3 has minus 0.12, topic_4 is almost zero, and topic_5 is weakly positive at 0.07. This indicates that most individual topics do not have a strong linear association with price.

- Review score rating has only a weak positive correlation with price.
The correlation between review_scores_rating and price is 0.13, meaning higher-rated listings are only slightly associated with higher prices. Price appears to be influenced more by property size than by rating alone.
Availability and number of reviews have almost no direct relationship with price.

- availability_365 has a correlation close to zero with price, while number_of_reviews has a slight negative correlation of minus 0.04. This suggests that booking availability and review count are not strong standalone indicators of listing price.

- Some topic features are negatively correlated with each other.
topic_1 and topic_4 have a strong negative correlation of minus 0.56, while topic_3 and topic_4 have minus 0.39. This shows that different review themes may represent distinct or competing patterns in the text data.
Overall, the most important linear price drivers appear to be physical listing attributes.

- The correlation matrix suggests that accommodates, bedrooms, and bathrooms are the strongest direct predictors of price, while NLP-based features such as sentiment, zero-shot scores, and topic scores may provide additional contextual signals for advanced models.

#### Geospatial Plot
"""

# Create price categories based on Airbnb listing prices
listings_df['price_range'] = pd.cut(
    listings_df['price'],
    bins = [0, 100, 200, 300, listings_df['price'].max()],
    labels = ['< $100', '$100–$200', '$200–$300', '>$300']
)

# Create an interactive map of listings by location and price range
fig = px.scatter_mapbox(
    listings_df,
    lat = 'latitude',
    lon = 'longitude',
    color = 'price_range',
    hover_data = ['price', 'bedrooms', 'bathrooms'],
    zoom = 10,
    mapbox_style = "open-street-map",
    title = "Airbnb Listings in Chicago, IL by Price Range"
)

# Display the interactive map
fig.show()

"""***`Observations:`***

- The geospatial plot shows that most Airbnb listings are concentrated within Chicago city limits, especially near the central and northern parts of the city.

- Listings are not evenly distributed across the map. There is a dense cluster around downtown Chicago and nearby lakefront neighborhoods, indicating higher Airbnb activity in popular urban and tourist areas.

- Lower-priced listings, especially those below 100, appear across many parts of the city, showing that budget-friendly options are widely available.

- Listings priced below 300 make up a large portion of the dataset and are spread across both central and surrounding neighborhoods.

- Higher-priced listings are less frequent and appear more concentrated in specific high-demand areas, suggesting that location may play an important role in pricing.

- The map indicates that proximity to central Chicago and lakefront areas may be associated with higher listing density and potentially stronger rental demand.

- The geographic spread of listings supports the use of latitude and longitude as useful location-based features for the price prediction model.

### Data Pre-Processing

#### Handling Outliers of Independent Variables
"""

# Calculate the first and third quartiles for numeric features
Q1 = listings_df[Numeric_Columns].quantile(0.25)
Q3 = listings_df[Numeric_Columns].quantile(0.75)

# Calculate the interquartile range for each numeric feature
IQR = Q3 - Q1

# Define lower and upper limits for detecting outliers using the IQR method
lower_bound = Q1 - 1.5 * IQR
upper_bound = Q3 + 1.5 * IQR

# Calculate the percentage of outliers in each numeric feature
Outliers_Percentage = (((listings_df[Numeric_Columns] < lower_bound) | (listings_df[Numeric_Columns] > upper_bound)).sum() / listings_df.shape[0]) * 100

# Display outlier percentage for all numeric columns
print(f'Percentage of Outliers in the Numerical Columns: \n{Outliers_Percentage}')

"""***`Observations:`***

- The IQR method was used to detect outliers in the numerical columns by calculating the first quartile, third quartile, interquartile range, lower bound, and upper bound for each feature.

- Several review-based NLP features show a high percentage of outliers, especially Service with 22.11, Safety with 21.90, Location with 19.51, Cleanliness with 19.45, Amenities with 17.63, and huggingface_score with 14.82. This is expected because most review scores are concentrated near high values, while lower scores appear as unusual cases.

- Among the topic modeling features, topic_2 has 21.12 outliers, topic_5 has 15.16 outliers, and topic_3 has 8.46 outliers, indicating that some topics are dominant only for a smaller group of listings. Topic_1 and topic_4 show 0 outliers, suggesting more stable distributions based on the IQR rule.

- Host and review-related features also contain noticeable outliers. host_acceptance_rate has 12.86 outliers, number_of_reviews has 6.82 outliers, and review_scores_rating has 5.13 outliers, showing that some listings have unusually low acceptance rates, very high review counts, or unusually low ratings.

- Property-related features have fewer outliers compared to NLP and review features. accommodates has 3.98 outliers, bathrooms has 1.90 outliers, and bedrooms has only 0.47 outliers, meaning most listings fall within a typical property size range.

- availability_365 has 0 outliers, which means the availability values are spread within the expected range and do not violate the IQR-based limits.

- The target variable price has 6.67 outliers, confirming the presence of unusually high-priced listings compared to the majority of properties. These extreme values may strongly influence model training and prediction errors.

- Since the dataset contains outliers across price, review scores, NLP scores, and topic features, appropriate outlier treatment such as capping or transformation can help reduce the effect of extreme values before building the prediction model.
"""

# Apply IQR based capping also known as Winsorization to selected columns
for col in ['host_acceptance_rate', 'review_scores_rating', 'number_of_reviews']:

    # Calculate the first quartile which represents the 25th percentile
    Q1 = listings_df[col].quantile(0.25)

    # Calculate the third quartile which represents the 75th percentile
    Q3 = listings_df[col].quantile(0.75)

    # Compute the Interquartile Range which measures spread of the middle 50 percent of data
    IQR = Q3 - Q1

    # Define the lower bound and upper bound for acceptable values based on IQR rule
    lower_bound = Q1 - 1.5 * IQR
    upper_bound = Q3 + 1.5 * IQR

    # Cap values below lower bound and above upper bound to limit the effect of outliers
    listings_df[col] = np.clip(listings_df[col], lower_bound, upper_bound)

"""***`Observations:`***

- IQR based outlier treatment was applied only to selected numerical columns: host_acceptance_rate, review_scores_rating, and number_of_reviews.

- These columns were selected because they showed noticeable outliers during the earlier outlier percentage check and boxplot analysis.

- The method calculates Q1, Q3, and IQR for each selected feature to define acceptable lower and upper bounds.

- Values below the lower bound and above the upper bound were capped instead of removed, which helps preserve the number of records in the dataset.

- Winsorization reduces the influence of extreme values while keeping the original data structure intact.

- This step is useful before model building because extreme values in features like review counts and ratings can disproportionately affect model learning.

- The target variable price was not capped in this step, so the outlier handling was focused only on selected independent variables.

#### Handling Outliers of Target Variable (Price)
"""

# Visualize listing price distribution before removing extreme high-price values
plt.figure(figsize=(10, 4))
sns.histplot(listings_df['price'], bins=50, kde=True, color='green')

# Add chart title and x-axis label
plt.title("Price Distribution (Before Removing Top 1%)")
plt.xlabel("Price")

# Display price distribution plot
plt.show()

"""***`Observations:`***

- The price distribution is highly right-skewed, meaning most listings are concentrated at lower price values while a few listings have very high prices.

- A large number of listings fall within the lower price range, showing that budget and mid-range listings dominate the dataset.

- The long tail on the right side indicates the presence of extreme high-price listings, which can strongly influence model training.

- These extreme values may increase prediction error because the model may try to fit unusually expensive listings that do not represent the majority of the data.

- Since the top 1 percent of high-price values are far away from the main price concentration, removing them can help reduce noise and make the target variable more stable.

- After removing extreme high-price values, the price distribution is expected to become more balanced and easier for machine learning models to learn from.
"""

# Calculate the 99th percentile price threshold
price_cap = listings_df['price'].quantile(0.99)

# Remove listings with prices above the 99th percentile
listings_df = listings_df[listings_df['price'] <= price_cap]

# Visualize listing price distribution after removing extreme high-price values
plt.figure(figsize=(10, 4))
sns.histplot(listings_df['price'], bins=50, kde=True, color='green')

# Add chart title and x-axis label
plt.title("Price Distribution (After Removing Top 1%)")
plt.xlabel("Price")

# Display updated price distribution plot
plt.show()

"""***`Observations:`***

- The price distribution before removing extreme values was highly right-skewed, meaning most listings had lower to moderate prices, while a small number of listings had very high prices.

- A few extreme high-price listings were stretching the price range up to around 4500, which could strongly influence the model and make it harder to learn general pricing patterns.

- The 99th percentile was used as a threshold to remove only the most extreme top 1 percent of price values, instead of removing a large portion of the dataset.

- After removing the top 1 percent of prices, the distribution became more compact and easier to interpret, with most listings concentrated below 400.

- The cleaned price distribution still remains right-skewed, but the extreme tail is reduced, making the target variable more suitable for regression modeling.

- This step helps reduce the impact of unusually expensive listings and allows the neural network model to focus more on typical Airbnb pricing behavior in Chicago.

***`Reasoning for not handling Outliers in Other Columns:`***

- Outliers were first checked across all numerical columns using the IQR method, but not every detected outlier needed to be removed or capped because some values represent valid Airbnb listing characteristics.

- Columns such as accommodates, bedrooms, and bathrooms can naturally have higher values because some Airbnb properties are large homes, shared spaces, or group accommodations. Removing these values could remove important premium property information.

- The review-based NLP features such as huggingface_score, Location, Amenities, Service, Cleanliness, and Safety are bounded score variables, mostly ranging between 0 and 1. Low or high values are meaningful confidence scores rather than data errors, so they were retained.

- The LDA topic features topic_1 to topic_5 represent topic probabilities or topic contribution scores. Since these values indicate how strongly each review belongs to a topic, extreme values are expected and meaningful for capturing review themes.

- availability_365 was not treated as an outlier because values from 0 to 365 are naturally valid. A listing available all year or unavailable for most of the year can reflect real host behavior.

- latitude and longitude were excluded from outlier treatment because they represent geographic coordinates. Their values are valid as long as they fall within the Chicago listing region, and modifying them would distort the actual location of the property.

- host_acceptance_rate, review_scores_rating, and number_of_reviews were handled using IQR-based capping because they had clear skewness and could influence model learning without needing to remove records.

- The price column was handled separately because it is the target variable. Extreme price values can heavily affect regression model training, so listings above the 99th percentile were removed to reduce target distortion while preserving most of the dataset.

#### Binary Mapping of Boolean-Like Columns
"""

# Convert binary categorical columns into numerical (0/1) format
listings_df['host_is_superhost'] = listings_df['host_is_superhost'].map({'t': 1, 'f': 0}) # Map 'host_is_superhost' from 't'/'f' to 1/0
listings_df['instant_bookable'] = listings_df['instant_bookable'].map({'t': 1, 'f': 0}) # Map 'instant_bookable' from 't'/'f' to 1/0

"""***`Observations:`***

- The host_is_superhost and instant_bookable columns were originally stored as categorical boolean-like values using t and f, so they needed to be converted into numerical format before model training.

- Binary mapping was applied by converting t to 1 and f to 0, which is suitable because both columns have only two possible categories and do not require one-hot encoding.

- This transformation preserves the meaning of both features while making them usable as independent variables in the neural network price prediction model.

#### Applying One-Hot Encoding to categorical column
"""

# Convert categorical 'room_type' into dummy/one-hot encoded variables
listings_df = pd.get_dummies(listings_df, columns = ['room_type'] , drop_first = True)

# Remove 'price_range' column as it's no longer needed
listings_df = listings_df.drop('price_range', axis = 1)

# Convert all columns to float type for modeling compatibility
listings_df = listings_df.astype(float)

# Display first few rows of the processed dataframe
listings_df.head()

"""***`Observations:`***

- The boolean-like categorical variables, host_is_superhost and instant_bookable, were converted from t and f values into numeric 1 and 0 values. This makes the variables suitable for machine learning models while preserving their original meaning.

- The room_type column was converted using one-hot encoding because it contains multiple categories such as entire home or apartment, private room, shared room, and hotel room. Since these categories do not have a natural order, dummy encoding is appropriate. drop_first = True was used to avoid multicollinearity by removing one reference category.

- The price_range column was removed because it was created only for EDA and visualization purposes. Since the actual target variable for the regression model is price, keeping price_range could introduce redundant information or data leakage. Finally, all columns were converted to float format to ensure compatibility with machine learning and neural network models.

### Data Preparation for Model Building
"""

# Split dataset into features (X) and target variable (y)
X = listings_df.drop(['price', 'latitude', 'longitude'], axis = 1)
# 'price' is the target variable (what we are trying to predict), so it must not be included in features
# 'latitude' and 'longitude' are dropped to avoid high-cardinality noise or because location is not being used explicitly in this model

y = listings_df['price']

# Split data into training and testing sets (70% train, 30% test)
X_train, X_test, y_train, y_test = train_test_split(
    X,
    y,
    test_size = 0.3,
    random_state = 1
)

"""***`Observations:`***

- The dataset was separated into independent variables and target variable, where price was selected as the target because the objective of the model is to predict Airbnb listing prices.

- The price column was removed from the feature set to avoid data leakage, since including the target variable as an input would make the model learn the answer directly instead of learning meaningful pricing patterns.

- The latitude and longitude columns were also removed from the model input. Although these columns represent location, they can introduce high-cardinality spatial noise and may not be useful unless location is handled more explicitly through engineered features such as neighborhood clusters, distance from downtown, or tourist-area indicators.

- The remaining processed variables were stored in X, while the target variable was stored separately in y, making the dataset ready for supervised machine learning model training.

- The data was split into training and testing sets using a 70 to 30 ratio. This allows the model to learn patterns from the training data and then be evaluated on unseen test data to check how well it generalizes.

- A fixed random_state value was used to ensure reproducibility, meaning the same train-test split can be generated every time the notebook is run.
"""

# Display the shapes of training and testing feature sets
print('Shape of Training Set:', X_train.shape)
print('Shape of Testing Set:', X_test.shape)

"""***`Observations:`***

- The categorical variable room_type was converted into dummy variables using one-hot encoding, which makes it suitable for machine learning models. The drop_first = True option was used to avoid duplicate information among the room type categories and reduce multicollinearity.

- The price_range column was removed because it was only created for EDA and map visualization. Since the actual target variable for model building is price, keeping price_range would not add useful predictive value and could create data leakage.

- All columns were converted to float type to ensure modeling compatibility, especially for neural networks, which require numerical input features in a consistent format.

- The dataset was split into independent variables and target variable, where price was selected as the target variable and removed from the feature set. This ensures the model learns to predict price from listing, host, review, sentiment, zero-shot, and topic-based features.

- latitude and longitude were also removed from the feature set before model building. Although they represent location, raw coordinate values can introduce high-cardinality noise and may not be directly interpretable unless transformed into meaningful location-based features such as neighborhood clusters or distance from key city areas.

- The data was split into training and testing sets using a 70 to 30 ratio. The training set contains 2660 records and 23 features, while the testing set contains 1141 records and 23 features. This split allows the model to learn patterns from the training data and evaluate generalization performance on unseen data.
"""

# Initialize and adapt normalization layer using training data
normalization_layer = Normalization()
normalization_layer.adapt(X_train.values)

"""***`Observations:`***

- A normalization layer was initialized to scale the input features before training the neural network model.

- The layer was adapted only on the training data, which is the correct approach because it prevents information from the test set from leaking into the model training process.

- Normalization helps bring all numerical features to a comparable scale, which is important here because the dataset contains variables with very different ranges, such as accommodates, number_of_reviews, availability_365, sentiment scores, topic scores, and encoded categorical variables.

- This preprocessing step can improve neural network training stability by helping the model learn faster and reducing the chance that large-scale variables dominate smaller-scale variables.

- Since the normalization layer is fitted on X_train.values, the same learned scaling parameters can later be applied consistently to both training and testing data during model training and evaluation.

### ***Building Neural Network Regression Models using Keras Sequential***

#### ***Neural Networks Model 1***
"""

# Build the first neural network regression model
nn_model1 = Sequential([

    # Define input layer based on the number of training features
    Input(shape = (X_train.shape[1], )),

    # Add the normalization layer
    normalization_layer,

    # Add hidden dense layers with ReLU activation
    Dense(128, activation = 'relu'),
    Dense(64, activation = 'relu'),
    Dense(32, activation = 'relu'),

    # Output layer for predicting Airbnb listing price
    Dense(1)
])

"""***`Observations:`***

- The neural network is designed as a regression model to predict Airbnb listing prices, with the input layer dynamically matching the number of selected features (23). This ensures the model can effectively utilize all engineered features, including listing attributes, review metrics, sentiment scores, and topic-based features.

- A normalization layer is included at the beginning of the model to standardize input features before training. This is useful when features have different scales; however, since the dataset was already normalized during preprocessing, this step is redundant and may result in double normalization.

- The model architecture consists of three fully connected hidden layers with 128, 64, and 32 neurons using ReLU activation. This layered structure allows the model to capture non-linear relationships between features such as amenities, reviews, and perceived experience, which are important drivers of pricing.

- The final output layer contains a single neuron with no activation function, which is appropriate for a regression task where the goal is to predict a continuous variable (price).

- Overall, the model is structured to learn complex interactions between structured features and unstructured insights (from topic modeling and sentiment analysis), making it suitable for capturing real-world Airbnb pricing dynamics.
"""

# Compile the neural network regression model
nn_model1.compile(
    optimizer = 'adam',
    loss = 'mse',
    metrics = ['mae', 'mse']
)

# Display neural network model architecture
nn_model1.summary()

"""***`Observations:`***

- The model is compiled using the Adam optimizer with Mean Squared Error as the loss function, while both MAE and MSE are tracked as evaluation metrics, making it well suited for a regression task such as predicting Airbnb listing prices in Chicago.

- The architecture follows a funnel-shaped, fully connected feedforward design with a Normalization layer at the input, three progressively shrinking hidden layers, and a single-node output layer, reflecting a structured dimensionality reduction approach.

- The Normalization layer accepts 23 input features and holds 47 non-trainable parameters representing the mean and variance of each feature, ensuring all inputs are scaled to a comparable range before being passed into the dense layers for stable training.

- The first hidden layer expands the input into 128 neurons with 3,072 parameters, the second compresses it to 64 neurons with 8,256 parameters, and the third further reduces it to 32 neurons with 2,080 parameters, forming a progressive feature abstraction path.

- The output layer produces a single continuous value with 33 parameters, which is fully consistent with the regression objective of generating one predicted price value for each Airbnb listing based on the structured property and review-derived input features.

- The total parameter count of 13,488, of which 13,441 are trainable, makes this a relatively lightweight model that reduces the risk of overfitting, which is particularly beneficial given the tabular and moderately sized nature of the Airbnb listings dataset.
"""

# Visualize the neural network architecture with layer output shapes
tf.keras.utils.plot_model(nn_model1, show_shapes = True)

"""***`Observations:`***

- The architecture diagram visually confirms the sequential flow of the neural network, beginning with a Normalization layer and passing through four Dense layers in a top-down manner, with each layer's output serving as the input to the subsequent layer.

- The Normalization layer receives 23 input features and outputs the same shape of 23, indicating that it performs feature scaling without altering the dimensionality of the input, preserving all original features in a standardized form.

- The first Dense layer takes the 23 normalized features as input and maps them to 128 output neurons, representing a significant expansion of the feature space that enables the model to learn complex and high-dimensional patterns present in the Airbnb listings data.

- The second Dense layer receives the 128-dimensional representation and compresses it to 64 neurons, beginning the process of dimensionality reduction by consolidating the learned patterns into a more refined and compact feature representation.

- The third Dense layer further reduces the 64-dimensional input to 32 neurons, continuing the funnel-shaped compression strategy and distilling the most predictive signals from the earlier layers into a concise intermediate representation.

- The final Dense layer takes the 32-dimensional input and produces a single output value, which corresponds directly to the predicted listing price, confirming that the network is designed as a regression model with a scalar continuous output.
"""

# Train the first neural network model
history = nn_model1.fit(
    X_train,
    y_train,
    validation_split = 0.2,
    epochs = 100,
    batch_size = 32,
    verbose = 1
)

"""***`Observations:`***

- The model is trained using the fit function with the training features X_train and target variable y_train, following the standard supervised learning workflow for the Airbnb listing price regression task.

- A validation split of 0.2 is applied, meaning 20% of the training data is automatically reserved for validation during each epoch, allowing the model to monitor generalization performance without requiring a separate validation dataset.

- The model is trained for 100 epochs, providing sufficient iterations for the network to iteratively adjust its weights and converge toward an optimal solution for predicting listing prices.

- A batch size of 32 is used, which is a widely adopted default that strikes a balance between the noisy but fast updates of smaller batches and the stable but computationally expensive updates of larger batches, making it well suited for this dataset size.

- Setting verbose to 1 ensures that training progress is printed to the console after each epoch, displaying loss and metric values for both the training and validation sets, which is essential for monitoring convergence and detecting potential overfitting during training.
"""

# Plot training and validation MAE across epochs
plt.plot(history.history['mae'], label='Train MAE')
plt.plot(history.history['val_mae'], label='Validation MAE')

# Add chart labels and title
plt.xlabel('Epochs')
plt.ylabel('MAE')
plt.title('Training vs Validation MAE')

# Display legend, grid, and MAE plot
plt.legend()
plt.grid(True)
plt.show()

"""***`Observations:`***

- Both the Train MAE and Validation MAE begin at a very high value of approximately 165 to 170 in the first epoch, after which both curves drop sharply within the first 10 epochs, indicating that the model learns the most significant patterns in the Airbnb pricing data very rapidly during the early stages of training.

- After the initial steep decline, both curves stabilize in the range of 55 to 65 by approximately epoch 10, suggesting that the model reaches a reasonable level of predictive accuracy relatively quickly before entering a phase of slower and more gradual improvement.

- The Train MAE continues to decline steadily throughout the remaining epochs, reaching approximately 40 by epoch 100, which demonstrates that the model is consistently improving its fit to the training data as training progresses over the full 100 epochs.

- The Validation MAE, in contrast, stabilizes and fluctuates in the range of approximately 55 to 68 after epoch 10 and shows no meaningful downward trend beyond that point, indicating that the model's ability to generalize to unseen data plateaus much earlier than its performance on the training set.

- A growing divergence between the Train MAE and Validation MAE becomes increasingly visible from around epoch 40 onward, with the gap widening progressively toward epoch 100, which is a classic indication of overfitting where the model begins to memorize the training data rather than learning generalizable patterns.

- The noisy fluctuations observed in the Validation MAE curve throughout training suggest high variance in the validation set predictions, which may be attributed to the relatively small size of the validation subset, the presence of price outliers in the Airbnb dataset, or the model's sensitivity to the specific listings sampled for validation.
"""

# Plot training and validation loss across epochs
plt.plot(history.history['loss'], label='Train Loss (MSE)')
plt.plot(history.history['val_loss'], label='Validation Loss (MSE)')

# Add chart labels and title
plt.xlabel('Epochs')
plt.ylabel('MSE')
plt.title('Training vs Validation Loss')

# Display legend, grid, and loss plot
plt.legend()
plt.grid(True)
plt.show()

"""***`Observations:`***

- Both the Train Loss and Validation Loss begin at extremely high MSE values, with the Train Loss starting at approximately 45,000 and the Validation Loss at approximately 24,000, after which both curves drop sharply within the first 5 to 10 epochs, reflecting rapid initial learning of the dominant pricing patterns in the Airbnb listings data.

- The significant difference in the starting MSE values between the Train Loss and Validation Loss in the very first epoch suggests that the model's initial weight configuration produced considerably worse predictions on the training set compared to the validation set before any meaningful learning had occurred.

- After the steep initial decline, both curves converge and stabilize in the range of approximately 6,000 to 8,000 MSE by around epoch 10, indicating that the model quickly reaches a reasonable baseline level of performance on both the training and validation data.

- The Train Loss continues to decline gradually and consistently beyond epoch 10, reaching approximately 4,000 MSE by epoch 100, demonstrating that the model continues to improve its fit to the training data throughout the full duration of the training process.

- The Validation Loss, however, stabilizes and begins to show a slight upward drift from approximately epoch 50 onward, reaching close to 10,000 MSE by epoch 100, which indicates that the model's generalization performance on unseen data deteriorates in the later stages of training.

- The widening gap between Train Loss and Validation Loss from approximately epoch 40 onward is a clear and consistent signal of overfitting, suggesting that the model would benefit from regularization techniques such as dropout, L2 regularization, or early stopping to prevent the continued decline in generalization performance.

- The persistent fluctuations observed in the Validation Loss curve throughout training are consistent with the noisy behavior also seen in the Validation MAE plot, further reinforcing that high variance in validation set predictions is a recurring characteristic of this model's training dynamics on the Airbnb dataset.

##### Model Performance Evaluation

###### Performance on the Training Set
"""

# Evaluate the first neural network model on the training set
train_loss, train_mae, train_mse = nn_model1.evaluate(X_train, y_train)

# Generate price predictions for the training data
y_pred = nn_model1.predict(X_train).flatten()

# Calculate training performance metrics
train_r2 = r2_score(y_train, y_pred)
train_mape = np.mean(np.abs((y_train - y_pred) / y_train)) * 100
train_rmse = np.sqrt(train_mse)

# Get sample size and number of predictors for adjusted R-squared
n = X_train.shape[0]
k = X_train.shape[1]

# Calculate adjusted R-squared for training data
train_adj_r2 = 1 - ((1 - train_r2) * (n - 1) / (n - k -1))

# Store training performance metrics in a dataframe
nn_model1_Training_Performance = pd.DataFrame({
    'MAE': train_mae,
    'MAPE': train_mape,
    'MSE': train_mse,
    'RMSE': train_rmse,
    'R2 Score': train_r2,
    'Adjusted R2 Score': train_adj_r2
}, index = [0])

# Display training performance results
nn_model1_Training_Performance

"""***`Observations:`***

- The model achieves a Mean Absolute Error of 43.64, indicating that on average the predicted Airbnb listing price deviates from the actual price by approximately $43.64, which represents a moderate level of prediction error given the wide price range typically observed in the Chicago Airbnb market.

- The Mean Absolute Percentage Error of 28.10% suggests that the model's predictions deviate from the actual prices by roughly 28% on average, which indicates that while the model captures the general pricing trends, it still carries a notable degree of relative error that may limit its practical utility for precise price recommendations.

- The Mean Squared Error of 4,528.46 and the corresponding Root Mean Squared Error of 67.29 indicate that the model is sensitive to larger prediction errors, as the RMSE is considerably higher than the MAE, suggesting the presence of high-priced listings or outliers in the Chicago dataset that the model struggles to predict accurately.

- The R2 Score of 0.7424 indicates that the model explains approximately 74.24% of the variance in Airbnb listing prices, reflecting a reasonably strong explanatory power and suggesting that the selected features, including structured property attributes and review-derived indicators, collectively provide meaningful predictive signal.

- The Adjusted R2 Score of 0.7401 is only marginally lower than the R2 Score, confirming that the inclusion of the 23 input features is justified and that there is minimal penalty for feature count, meaning the model does not suffer from unnecessary feature inflation that would otherwise reduce the adjusted metric significantly.

- The close alignment between the R2 Score and the Adjusted R2 Score further validates the feature selection process, suggesting that the combination of structural listing attributes and NLP-derived sentiment and topic scores contributes meaningfully to the model's predictive performance without introducing redundant or uninformative variables.

###### Performance on the Test Set
"""

# Evaluate the first neural network model on the test set
test_loss, test_mae, test_mse = nn_model1.evaluate(X_test, y_test)

# Generate price predictions for the test data
y_pred = nn_model1.predict(X_test).flatten()

# Calculate test performance metrics
test_r2 = r2_score(y_test, y_pred)
test_mape = np.mean(np.abs((y_test - y_pred) / y_test)) * 100
test_rmse = np.sqrt(test_mse)

# Get sample size and number of predictors for adjusted R-squared
n = X_test.shape[0]
k = X_test.shape[1]

# Calculate adjusted R-squared for test data
test_adj_r2 = 1 - ((1 - test_r2) * (n - 1) / (n - k -1))

# Store test performance metrics in a dataframe
nn_model1_Test_Performance = pd.DataFrame({
    'MAE': test_mae,
    'MAPE': test_mape,
    'MSE': test_mse,
    'RMSE': test_rmse,
    'R2 Score': test_r2,
    'Adjusted R2 Score': test_adj_r2,
}, index = [0])

# Display test performance results
nn_model1_Test_Performance

"""***`Observations:`***

- The model achieves a test MAE of 61.72, which is notably higher than the training MAE of 43.64, indicating a meaningful degradation in prediction accuracy when the model is applied to completely unseen Airbnb listings, which is consistent with the overfitting behavior observed in the training curves.

- The test MAPE of 41.05% is substantially higher than the training MAPE of 28.10%, suggesting that the model's relative prediction error increases considerably on unseen data, implying that the model has not generalized well enough to reliably estimate prices as a percentage of their true values across the test set.

- The test MSE of 7,889.82 and the corresponding RMSE of 88.82 are both significantly higher than their training counterparts of 4,528.46 and 67.29 respectively, further confirming that the model's sensitivity to large prediction errors is amplified on the test set, likely due to the influence of high-priced listing outliers that were not adequately represented during training.

- The test R2 Score of 0.5416 indicates that the model explains only approximately 54.16% of the variance in listing prices on the test set, which is a considerable drop from the training R2 Score of 0.7424, highlighting that a significant portion of the pricing variability in unseen data remains unexplained by the current model.

- The Adjusted R2 Score of 0.5321 on the test set is slightly lower than the R2 Score, and the gap between the two metrics is marginally wider compared to the training set, suggesting that the penalty for the number of features becomes slightly more pronounced when the model is evaluated on data it has not been trained on.

- The substantial gap between training and test performance across all metrics collectively reinforces the overfitting concern identified during training, and suggests that the model would benefit from regularization strategies such as dropout layers, early stopping, or weight decay to improve its generalization capability on unseen Chicago Airbnb listing data.

#### ***Neural Networks Model 2***
"""

# Build the second neural network model with regularization
nn_model2 = Sequential([

    # Define input layer based on training feature count
    Input(shape = (X_train.shape[1],)),

    # Add the normalization layer
    normalization_layer,

    # First hidden layer with L2 regularization, batch normalization, and dropout
    Dense(256, activation = 'relu', kernel_regularizer = l2(0.0004)),
    BatchNormalization(),
    Dropout(0.3),

    # Second hidden layer with L2 regularization, batch normalization, and dropout
    Dense(128, activation = 'relu', kernel_regularizer = l2(0.0004)),
    BatchNormalization(),
    Dropout(0.2),

    # Third hidden layer with L2 regularization, batch normalization, and dropout
    Dense(64, activation = 'relu', kernel_regularizer = l2(0.0004)),
    BatchNormalization(),
    Dropout(0.1),

    # Fourth hidden layer with L2 regularization, batch normalization, and dropout
    Dense(32, activation = 'relu', kernel_regularizer = l2(0.0004)),
    BatchNormalization(),
    Dropout(0.1),

    # Output layer for Airbnb price prediction
    Dense(1)
])

"""***`Observations:`***

- The second neural network model is built with a deeper and more regularized architecture compared to the first model, incorporating four hidden layers instead of three, along with Batch Normalization and Dropout after each hidden layer, reflecting a deliberate effort to address the overfitting issues observed in the first model.

- The first hidden layer is expanded to 256 neurons, doubling the capacity of the first model's initial layer, which allows the network to learn a richer and more diverse set of feature representations from the 23 input features derived from Airbnb listing attributes and review-based NLP indicators.

- L2 regularization with a penalty coefficient of 0.0004 is applied to the kernel weights of all four hidden layers, which discourages the model from assigning excessively large weights to any particular feature and helps reduce overfitting by keeping the learned weight values small and more generalizable.

- Batch Normalization is applied after each hidden layer's activation, which normalizes the outputs of each layer during training, stabilizes the gradient flow, accelerates convergence, and reduces the model's sensitivity to weight initialization, collectively contributing to more stable and efficient training dynamics.

- The Dropout rates are applied in a decreasing fashion across the layers, with 0.3 in the first hidden layer, 0.2 in the second, and 0.1 in both the third and fourth layers, reflecting a strategy of applying stronger regularization in the earlier and wider layers where overfitting risk is higher, while preserving more information in the deeper and narrower layers.

- The output layer retains a single neuron with no activation function, which is consistent with the regression objective of predicting a single continuous Airbnb listing price value, and remains identical in design to the output layer used in the first neural network model.
"""

# Compile the second neural network regression model
nn_model2.compile(
    optimizer = Adam(learning_rate = 0.001),
    loss = 'mse',
    metrics = ['mae', 'mse']
)

# Display model architecture and layer details
nn_model2.summary()

"""***`Observations:`***

- The second model is compiled using the Adam optimizer with an explicitly defined learning rate of 0.001, which is the well-established default value for Adam and is known to provide a reliable balance between convergence speed and training stability for regression tasks such as Airbnb price prediction.

- Unlike the first model where the optimizer was specified simply as the string 'adam', the second model instantiates the Adam optimizer as an object with an explicit learning rate parameter, offering greater transparency and flexibility for future tuning of the learning rate if required during experimentation.

- Mean Squared Error is retained as the loss function, maintaining consistency with the first model's training objective and ensuring that the optimization process continues to penalize larger prediction errors more heavily, which is particularly relevant given the wide price distribution observed in the Chicago Airbnb listings dataset.

- Both MAE and MSE are tracked as evaluation metrics alongside the loss, allowing for a comprehensive assessment of model performance during training and validation, where MAE provides an interpretable measure of average prediction error in dollar terms and MSE captures sensitivity to larger deviations.

- The model summary is displayed using the summary function, which will provide a detailed breakdown of each layer's type, output shape, and parameter count, enabling a direct architectural comparison between the first and second neural network models with respect to depth, width, and total trainable parameters.
"""

# Visualize the architecture of the second neural network model
tf.keras.utils.plot_model(nn_model2, show_shapes = True)

"""***`Observations:`***

- The architecture diagram confirms the sequential and layered flow of the second neural network model, where data passes through a total of 13 distinct layers including Normalization, Dense, BatchNormalization, and Dropout layers, representing a significantly more complex architecture compared to the five-layer design of the first model.

- The Normalization layer receives 23 input features and outputs the same shape of 23, maintaining dimensional consistency at the entry point of the network and ensuring that all input features are standardized before being passed into the first Dense layer.

- The first Dense layer expands the 23 normalized features to 256 output neurons, achieving the largest dimensional expansion in the network and enabling the model to capture a rich and high-dimensional set of feature interactions from the Airbnb listing and review-derived input variables.

- Each Dense layer is immediately followed by a BatchNormalization layer that preserves the output dimensionality, confirming that normalization is applied directly to the activated outputs before any neurons are dropped, which ensures stable and well-scaled inputs are passed to the subsequent Dropout layer.

- The Dropout layers following each BatchNormalization block also preserve the input dimensionality, confirming that Dropout does not alter the shape of the representation but selectively deactivates neurons during training to reduce co-dependency between units and improve generalization.

- The four Dense layers progressively reduce the feature dimensionality from 256 to 128, then from 128 to 64, and finally from 64 to 32, following a consistent funnel-shaped compression pattern that systematically distills the most predictive information across successive layers before reaching the output.

- The final Dense layer takes the 32-dimensional representation as input and produces a single scalar output, which directly corresponds to the predicted Airbnb listing price and confirms the regression nature of the model with a linear output node carrying no activation function.
"""

# Define early stopping to prevent overfitting during training
early_stop = EarlyStopping(
    monitor = 'val_loss',
    patience = 20,
    restore_best_weights = True
)

"""***`Observations:`***

- An EarlyStopping callback is defined to monitor the validation loss during training, which directly addresses the overfitting behavior observed in the first model where the validation loss began rising while the training loss continued to decrease beyond epoch 40.

- The patience parameter is set to 20, meaning the training process will continue for up to 20 consecutive epochs without any improvement in validation loss before terminating, striking a balance between allowing sufficient time for the model to escape temporary plateaus and preventing unnecessary training that leads to overfitting.

- The restore_best_weights parameter is set to True, ensuring that upon early stopping the model's weights are automatically reverted to the state corresponding to the epoch with the lowest validation loss, rather than retaining the weights from the final training epoch which may represent an overfitted state.

- The combination of monitoring validation loss with a patience of 20 and restoring the best weights collectively ensures that the final model delivered after training is the one that generalizes best to unseen Airbnb listing data, rather than the one that simply performed best on the training set at the last recorded epoch.

- This early stopping strategy is particularly well suited for the second model given its increased architectural complexity with 256 neurons in the first layer, multiple Dropout and BatchNormalization layers, and L2 regularization, where the optimal stopping point may vary across training runs depending on the inter
"""

# Train the second neural network model with callbacks
history = nn_model2.fit(
    X_train,
    y_train,
    validation_split = 0.2,
    epochs = 300,
    batch_size = 32,
    callbacks = early_stop,
    verbose = 1
)

"""***`Observations:`***

- The second model is trained for a maximum of 300 epochs, which is three times the number of epochs used for the first model, reflecting the expectation that the additional regularization mechanisms such as Dropout, Batch Normalization, and L2 regularization may slow down convergence and require more iterations to reach an optimal solution.

- The early stopping callback is passed through the callbacks parameter, ensuring that training will automatically terminate before reaching 300 epochs if the validation loss fails to improve for 20 consecutive epochs, effectively preventing the model from training longer than necessary and avoiding the overfitting observed in the first model.

- A validation split of 0.2 is maintained, consistent with the first model's training configuration, where 20% of the training data is held out for validation at each epoch to monitor the model's generalization performance and serve as the signal for the early stopping mechanism.

- The batch size of 32 is retained from the first model, ensuring that the stochastic gradient updates remain consistent across both models and that any differences in training dynamics and final performance can be more confidently attributed to the architectural and regularization changes introduced in the second model.

- The combination of a 300 epoch ceiling with early stopping and best weight restoration creates a flexible and self-regulating training process, where the model is given ample opportunity to converge fully while being protected against overfitting through automatic termination and weight recovery at the optimal generalization checkpoint.
"""

# Plot training and validation MAE across epochs
plt.plot(history.history['mae'], label='Train MAE')
plt.plot(history.history['val_mae'], label='Validation MAE')

# Add chart labels and title
plt.xlabel('Epochs')
plt.ylabel('MAE')
plt.title('Training vs Validation MAE')

# Display legend, grid, and MAE plot
plt.legend()
plt.grid(True)
plt.show()

"""***`Observations:`***

- Both the Train MAE and Validation MAE begin at relatively high values of approximately 182 and 172 respectively, after which both curves decline together in a smooth and gradual manner from the very first epoch, which is notably different from the abrupt initial drop observed in the first model and reflects the stabilizing effect of Batch Normalization on the early training dynamics.

- Unlike the first model where the initial descent was extremely steep and completed within the first 5 to 10 epochs, the second model exhibits a prolonged and steady decline spanning approximately 30 epochs, suggesting that the combined effect of Dropout, Batch Normalization, and L2 regularization slows down convergence while promoting a more controlled and stable learning trajectory.

- The two curves remain closely aligned throughout the entire descent from epoch 0 to approximately epoch 30, with the Validation MAE closely tracking the Train MAE at every stage, which is a strong indicator that the regularization mechanisms in the second model are successfully preventing the model from overfitting to the training data during the learning phase.

- Both curves converge to approximately 60 MAE around epoch 30, after which the Train MAE continues to decline slightly and stabilizes near 50, while the Validation MAE plateaus and fluctuates within a narrow range of approximately 55 to 60, indicating that the model reaches its generalization limit around this point.

- The early stopping mechanism appears to have terminated training at approximately epoch 60, which is well within the 300 epoch ceiling, confirming that the validation loss stopped improving sufficiently and that the best weights were restored from the optimal checkpoint identified during training.

- The overall gap between Train MAE and Validation MAE in the second model is substantially smaller compared to the first model, where the divergence was more pronounced and continued to widen through epoch 100, demonstrating that the regularization strategies employed in the second model have effectively improved the balance between training performance and generalization.
"""

# Plot training and validation loss across epochs
plt.plot(history.history['loss'], label='Train Loss (MSE)')
plt.plot(history.history['val_loss'], label='Validation Loss (MSE)')

# Add chart labels and title
plt.xlabel('Epochs')
plt.ylabel('MSE')
plt.title('Training vs Validation Loss')

# Display legend, grid, and loss plot
plt.legend()
plt.grid(True)
plt.show()

"""***`Observations:`***

- Both the Train Loss and Validation Loss begin at high MSE values of approximately 50,500 and 44,000 respectively, and both curves decline together in a smooth and gradual manner from the very first epoch, reflecting the stabilizing influence of Batch Normalization and the controlled learning dynamics introduced by the regularization mechanisms in the second model.

- The Validation Loss starts lower than the Train Loss in the first epoch, which is consistent with the behavior observed in the first model and can be attributed to the effect of Dropout being active only during training, causing the training loss to appear relatively higher in the early epochs as neurons are randomly deactivated during forward passes.

- Both curves follow a closely aligned and smooth descent from epoch 0 through approximately epoch 30, with no abrupt drops or erratic fluctuations, which strongly contrasts with the steep and noisy decline observed in the first model and confirms that the combination of L2 regularization, Batch Normalization, and Dropout promotes a significantly more stable and controlled optimization trajectory.

- Both curves converge to approximately 7,000 to 9,000 MSE around epoch 30, after which the Train Loss continues to decline gradually and stabilizes near 5,000 MSE, while the Validation Loss plateaus and fluctuates within a narrow band of approximately 7,000 to 9,000 MSE, indicating that the model reaches its generalization capacity around this point.

- The degree of divergence between Train Loss and Validation Loss in the second model after epoch 30 is considerably smaller than the divergence seen in the first model, where the gap continued to widen aggressively through epoch 100, confirming that the regularization strategies have meaningfully improved the model's ability to generalize to unseen Airbnb listing data.

- Training appears to have been terminated by the early stopping callback at approximately epoch 60, well before the 300 epoch maximum, indicating that the validation loss did not improve for 20 consecutive epochs beyond the optimal checkpoint, and that the best weights corresponding to the lowest validation loss were successfully restored for final model evaluation.

##### Model Performance Evaluation

###### Performace on the Training Set
"""

# Evaluate the second neural network model on the training set
train_loss, train_mae, train_mse = nn_model2.evaluate(X_train, y_train)

# Generate price predictions for the training data
y_pred = nn_model2.predict(X_train).flatten()

# Calculate training performance metrics
train_r2 = r2_score(y_train, y_pred)
train_mape = np.mean(np.abs((y_train - y_pred) / y_train)) * 100
train_rmse = np.sqrt(train_mse)

# Get sample size and number of predictors for adjusted R-squared
n = X_train.shape[0]
k = X_train.shape[1]

# Calculate adjusted R-squared for training performance
train_adj_r2 = 1 - ((1 - train_r2) * (n - 1) / (n - k -1))

# Store training performance metrics in a dataframe
nn_model2_Training_Performance = pd.DataFrame({
    'MAE': train_mae,
    'MAPE': train_mape,
    'MSE': train_mse,
    'RMSE': train_rmse,
    'R2 Score': train_r2,
    'Adjusted R2 Score': train_adj_r2
}, index = [0])

# Display training performance results
nn_model2_Training_Performance

"""***`Observations:`***

- The second model achieves a training MAE of 43.68, which is nearly identical to the first model's training MAE of 43.64, indicating that both models reach a comparable level of average prediction accuracy on the training set despite the second model employing a significantly more regularized and deeper architecture.

- The training MAPE of 27.66% is marginally lower than the first model's training MAPE of 28.10%, suggesting a slight improvement in relative prediction accuracy on the training data, though the difference is minimal and not indicative of a substantial gain in training performance between the two models.

- The training MSE of 4,539.80 and the corresponding RMSE of 67.38 are nearly identical to the first model's training MSE of 4,528.46 and RMSE of 67.29, confirming that both models exhibit very similar sensitivity to large prediction errors on the training set and that the regularization techniques have not compromised the model's capacity to fit the training data.

- The training R2 Score of 0.7417 is virtually the same as the first model's training R2 Score of 0.7424, indicating that the second model explains approximately 74.17% of the variance in Airbnb listing prices on the training set, demonstrating that the added regularization has preserved the model's explanatory power without any meaningful reduction in training performance.

- The Adjusted R2 Score of 0.7395 closely tracks the R2 Score, confirming that the 23 input features collectively contribute meaningful predictive signal and that there is no significant penalty for feature count, which is consistent with the feature validation observed in the first model as well.

- The near-identical training metrics across both models suggest that the key advantage of the second model lies not in improved training performance but in its expected improvement in generalization to unseen data, which will be more definitively assessed through the test set evaluation metrics.

###### Performance on the Test Set
"""

# Evaluate the second neural network model on the test set
test_loss, test_mae, test_mse = nn_model2.evaluate(X_test, y_test)

# Generate price predictions for the test data
y_pred = nn_model2.predict(X_test).flatten()

# Calculate test performance metrics
test_r2 = r2_score(y_test, y_pred)
test_mape = np.mean(np.abs((y_test - y_pred) / y_test)) * 100
test_rmse = np.sqrt(test_mse)

# Get sample size and number of predictors for adjusted R-squared
n = X_test.shape[0]
k = X_test.shape[1]

# Calculate adjusted R-squared for test performance
test_adj_r2 = 1 - ((1 - test_r2) * (n - 1) / (n - k -1))

# Store test performance metrics in a dataframe
nn_model2_Test_Performance = pd.DataFrame({
    'MAE': test_mae,
    'MAPE': test_mape,
    'MSE': test_mse,
    'RMSE': test_rmse,
    'R2 Score': test_r2,
    'Adjusted R2 Score': test_adj_r2,
}, index = [0])

# Display test performance results
nn_model2_Test_Performance

"""***`Observations:`***

- The second model achieves a test MAE of 53.72, which is a notable improvement over the first model's test MAE of 61.72, representing a reduction of approximately $8 in average prediction error on unseen Airbnb listings and confirming that the regularization strategies employed in the second model have meaningfully improved generalization performance.

- The test MAPE of 33.12% is lower than the first model's test MAPE of 41.05%, indicating that the second model's relative prediction error on unseen data is reduced by approximately 8 percentage points, reflecting a more consistent and proportionally accurate pricing prediction across listings of varying price levels in the Chicago Airbnb market.

- The test MSE of 7,139.11 and the corresponding RMSE of 84.49 are both lower than the first model's test MSE of 7,889.82 and RMSE of 88.82, confirming that the second model is less sensitive to large prediction errors on unseen data and handles high-priced listing outliers with relatively greater robustness.

- The test R2 Score of 0.5852 represents a meaningful improvement over the first model's test R2 Score of 0.5416, indicating that the second model explains approximately 58.52% of the variance in unseen Airbnb listing prices compared to 54.16% for the first model, demonstrating a tangible gain in generalization capability attributable to the deeper and more regularized architecture.

- The Adjusted R2 Score of 0.5766 closely follows the R2 Score, and the gap between the two metrics remains consistent with what was observed during training, confirming that the 23 input features continue to contribute meaningful predictive value without introducing unnecessary complexity that would otherwise penalize the adjusted metric on the test set.

- Comparing the train-to-test performance drop across both models, the second model shows a smaller degradation across all metrics, with the MAE gap narrowing from approximately 18 in the first model to approximately 10 in the second model, collectively confirming that the combination of L2 regularization, Batch Normalization, Dropout, and early stopping has successfully improved the model's ability to generalize to unseen Chicago Airbnb listing data.

### ***Building XGBoost Model***
"""

# Install XGBoost library for gradient boosting regression
!pip install xgboost

# XGBoost regression model
from xgboost import XGBRegressor

# Hyperparameter tuning using randomized search
from sklearn.model_selection import GridSearchCV

# Commented out IPython magic to ensure Python compatibility.
# %%time
# 
# # Initialize XGBoost regression model for hyperparameter tuning
# xgb_model = XGBRegressor(random_state = 1)
# 
# # Define hyperparameter search space for RandomizedSearchCV
# param_grid = {
#     "n_estimators": [100, 300, 500],
#     "learning_rate": [0.03, 0.05, 0.1],
#     "max_depth": [3, 4, 5],
#     "min_child_weight": [5, 10],
#     "subsample": [0.7, 0.9],
#     "colsample_bytree": [0.3, 0.5, 0.7, 0.9],
#     "gamma": [0.1, 0.2],
# }
# 
# # Define R-squared as the scoring metric for model tuning
# acc_scorer = metrics.make_scorer(metrics.r2_score)
# 
# # Configure randomized search with cross-validation
# grid_obj = GridSearchCV(
#     xgb_model,
#     param_grid,
#     cv = 5,
#     scoring = acc_scorer,
#     n_jobs = -1,
#     verbose = 2,
# )
# 
# # Fit randomized search to identify the best hyperparameters
# grid_obj = grid_obj.fit(X_train, y_train)
# 
# # Select the best XGBoost model from randomized search
# xgb_model = grid_obj.best_estimator_
# 
# # Train the tuned XGBoost model on the full training data
# xgb_model.fit(X_train, y_train)

"""***`Observations:`***

- An XGBoost Regressor is initialized with a fixed random state of 1 to ensure reproducibility of results across multiple runs, and it serves as the base estimator upon which hyperparameter tuning is performed using an exhaustive grid search strategy.

- The hyperparameter search space covers seven key parameters including the number of estimators, learning rate, maximum tree depth, minimum child weight, subsample ratio, column sample ratio, and gamma, collectively spanning the most influential dimensions of XGBoost model complexity, regularization, and learning behavior.

- The number of estimators is explored across three values of 100, 300, and 500, the learning rate across 0.03, 0.05, and 0.1, and the maximum depth across 3, 4, and 5, allowing the search to evaluate combinations that balance model capacity and computational efficiency for the Airbnb price prediction task.

- The min_child_weight values of 5 and 10 along with gamma values of 0.1 and 0.2 serve as regularization controls that prevent the model from creating overly specific tree splits on noisy or sparse regions of the Airbnb training data, thereby reducing the risk of overfitting.

- The subsample and colsample_bytree parameters introduce stochastic sampling of rows and features respectively during tree construction, with colsample_bytree explored across four values ranging from 0.3 to 0.9, adding randomness to the boosting process and further contributing to the model's generalization capability.

- R-squared is defined as the scoring metric using make_scorer, which ensures that all cross-validation folds during the grid search evaluate candidate hyperparameter combinations based on their ability to explain the variance in Airbnb listing prices, maintaining consistency with the evaluation framework used for the neural network models.

- A 5-fold cross-validation strategy is applied with all available CPU cores through n_jobs set to negative one, ensuring that the grid search is computationally efficient while producing robust and reliable estimates of model performance across different subsets of the training data.

- Upon completion of the grid search, the best estimator is extracted and subsequently retrained on the full training dataset, ensuring that the final tuned XGBoost model benefits from the maximum available training data rather than being limited to a subset used during cross-validation.

#### Model Performance Evaluation
"""

# Define function to calculate Mean Absolute Percentage Error
def mape_score(targets, predictions):
  return np.mean(np.abs((targets - predictions)/targets)) * 100

# Define function to calculate adjusted R-squared
def adj_r2_score(predictors, targets, predictions):
  r2 = r2_score(targets, predictions)
  n = predictors.shape[0]
  k = predictors.shape[1]

  return 1 - ((1 - r2) * (n - 1) / (n - k -1))

# Define function to evaluate regression model performance
def model_performance_evaluation(model, predictors, targets):

  # Generate predictions using the trained model
  predictions = model.predict(predictors)

  # Calculate regression performance metrics
  mse = mean_squared_error(targets, predictions)
  rmse = np.sqrt(mean_squared_error(targets, predictions))
  mae = mean_absolute_error(targets, predictions)
  mape = mape_score(targets, predictions)
  r2 = r2_score(targets, predictions)
  adj_r2 = adj_r2_score(predictors, targets, predictions)

  # Store performance metrics in a dataframe
  df_perf = pd.DataFrame({
    'MAE': mae,
    'MAPE': mape,
    'MSE': mse,
    'RMSE': rmse,
    'R2 Score': r2,
    'Adjusted R2 Score': adj_r2,
  }, index = [0])

  # Return model performance summary
  return df_perf

"""###### Model Performance on the Training Set"""

# Evaluate XGBoost model performance on the training set
xgb_model_training_performance = model_performance_evaluation(xgb_model, X_train, y_train)

# Display XGBoost training performance metrics
xgb_model_training_performance

"""***`Observations:`***

- The tuned XGBoost model achieves a training MAE of 36.37, which is considerably lower than the training MAE of both the first neural network model at 43.64 and the second at 43.68, indicating that XGBoost fits the training data with substantially greater accuracy and captures the underlying pricing patterns in the Airbnb listings dataset more effectively.

- The training MAPE of 25.78% is the lowest among all three models evaluated so far, suggesting that the XGBoost model produces proportionally more accurate price predictions on the training set and is better able to handle the relative variation in listing prices across different property types and neighborhoods in Chicago.

- The training MSE of 2,758.08 and the corresponding RMSE of 52.52 are significantly lower than those of both neural network models, where the first model recorded an RMSE of 67.29 and the second recorded 67.38, confirming that the XGBoost model is considerably less sensitive to large prediction errors and handles high-priced listing outliers more effectively on the training data.

- The training R2 Score of 0.8431 indicates that the XGBoost model explains approximately 84.31% of the variance in Airbnb listing prices on the training set, representing a substantial improvement of approximately 10 percentage points over both neural network models which achieved training R2 Scores of approximately 0.742, highlighting XGBoost's superior capacity to capture complex nonlinear relationships in the structured feature space.

- The Adjusted R2 Score of 0.8417 is only marginally lower than the R2 Score, confirming that all 23 input features contribute meaningful predictive information and that the model does not suffer from unnecessary feature inflation, which further validates the feature engineering process that incorporated structured listing attributes alongside NLP-derived sentiment and topic modeling scores.

- The close alignment between the R2 Score and the Adjusted R2 Score across the XGBoost model reinforces the quality of the feature set and suggests that the gradient boosting framework is able to effectively leverage both the structured property characteristics and the review-derived indicators to produce a well-fitted and parsimonious pricing model.

###### Model Performance on the Test Set
"""

# Evaluate XGBoost model performance on the test set
xgb_model_test_performance = model_performance_evaluation(xgb_model, X_test, y_test)

# Display XGBoost test performance metrics
xgb_model_test_performance

"""***`Observations:`***

- The XGBoost model achieves a test MAE of 53.71, which is lower than the first neural network model's test MAE of 61.72 but virtually identical to the second neural network model's test MAE of 53.72, indicating that while XGBoost generalizes better than the unregularized first model, it performs comparably to the regularized second model in terms of average absolute prediction error on unseen Airbnb listings.

- The test MAPE of 35.91% is higher than the second neural network model's test MAPE of 33.12%, suggesting that despite XGBoost's stronger training performance, the second neural network model produces relatively more accurate predictions as a proportion of actual listing prices when evaluated on unseen data, which may reflect XGBoost's tendency to overfit to the specific price distributions present in the training set.

- The test MSE of 6,598.25 and the corresponding RMSE of 81.23 are lower than both neural network models on the test set, where the first model recorded an RMSE of 88.82 and the second recorded 84.49, confirming that XGBoost remains less sensitive to large prediction errors on unseen data and handles high-priced listing outliers with greater robustness than either neural network model.

- The test R2 Score of 0.6166 represents an improvement over the first neural network model's test R2 Score of 0.5416 and the second model's score of 0.5852, indicating that the XGBoost model explains approximately 61.66% of the variance in unseen Airbnb listing prices, making it the best performing model in terms of explanatory power on the test set.

- The gap between the training R2 Score of 0.8431 and the test R2 Score of 0.6166 is approximately 0.23, which is notably larger than the corresponding gaps observed in both neural network models, suggesting that despite the hyperparameter tuning, the XGBoost model still exhibits a meaningful degree of overfitting to the training data relative to its generalization performance.

- The Adjusted R2 Score of 0.6087 on the test set is slightly lower than the R2 Score, and the modest gap between the two confirms that the 23 input features continue to provide meaningful predictive value without introducing excessive redundancy, though the overall test performance suggests that further regularization or feature refinement may be needed to reduce the train-to-test performance gap in the XGBoost model.
"""

# Extract feature importance scores from the trained XGBoost model
importances = xgb_model.feature_importances_

# Sort feature importances in ascending order
indices = np.argsort(importances)

# Store feature names from the input dataset
feature_names = list(X.columns)

# Plot feature importances for XGBoost model
plt.figure(figsize = (10, 7))
plt.title('Feature Importances')
plt.barh(range(len(indices)), importances[indices], color = 'violet', align = 'center')
plt.yticks(range(len(indices)), [feature_names[i] for i in indices])
plt.xlabel('Relative Importance')

# Display feature importance plot
plt.show()

"""***`Observations:`***

- The accommodates feature holds the highest relative importance of approximately 0.24, making it the single most influential predictor of Airbnb listing prices in Chicago, which aligns with the intuitive expectation that listings capable of hosting more guests command significantly higher prices in the short-term rental market.

- Bedrooms and bathrooms rank second and third with relative importances of approximately 0.20 and 0.12 respectively, collectively reinforcing that physical property size and capacity attributes are the dominant determinants of listing price, with the top three structural features alone accounting for over half of the model's total predictive signal.

- The room_type_Private room feature ranks fourth with a relative importance of approximately 0.10, indicating that the type of space offered has a substantial impact on pricing, with private rooms being priced distinctly lower than entire homes or apartments, reflecting the significant premium guests place on exclusive access to a property.

- Topic-based features derived from LDA topic modeling appear prominently in the rankings, with topic_2 and topic_3 ranking fifth and sixth respectively and topic_1, topic_4, and topic_5 also appearing in the mid-range, collectively demonstrating that latent thematic patterns extracted from guest reviews contribute meaningful predictive signal to the price prediction model beyond what structured features alone provide.

- The review_scores_rating feature appears in the upper mid-range of importance, confirming that overall guest satisfaction as expressed through review scores has a measurable influence on listing prices, suggesting that higher-rated listings are able to command a price premium in the Chicago Airbnb market.

- The zero-shot classification scores for Service, Location, Safety, Cleanliness, and Amenities all appear in the lower range of the importance ranking, indicating that while these NLP-derived guest perception indicators contribute to the model, their individual predictive contributions are relatively modest compared to structural property attributes and topic-based features.

- The huggingface_score, which represents the sentiment derived from the transformer-based model, appears in the lower mid-range alongside the zero-shot classification scores, confirming that sentiment analysis outputs contribute to the model's predictive capacity, though their influence is secondary to the structural and topic-based features in determining listing prices.

- The host-related features including host_acceptance_rate and host_is_superhost appear at the lower end of the importance ranking, suggesting that host characteristics have a limited but non-negligible influence on listing prices, with the superhost status contributing the least predictive signal among all 23 features included in the model.

### Model Comparison
"""

# Combine performance metrics from all trained models
model_comparison_df = pd.concat([
    nn_model1_Training_Performance.T,
    nn_model1_Test_Performance.T,
    nn_model2_Training_Performance.T,
    nn_model2_Test_Performance.T,
    xgb_model_training_performance.T,
    xgb_model_test_performance.T
], axis = 1)

# Assign clear column names for each model and dataset split
model_comparison_df.columns = ['NN Model 1 (Training)',
                               'NN Model 1 (Test)',
                               'NN Model 2 (Training)',
                               'NN Model 2 (Test)',
                               'XGB Model (Training)',
                               'XGB Model (Test)']

# Display model comparison table with models as rows
model_comparison_df.T

"""***`Observations:`***

- The consolidated comparison table reveals that the XGBoost model achieves the lowest training MAE of 36.37 and the highest training R2 Score of 0.8431 among all three models, confirming that gradient boosting with hyperparameter tuning captures the underlying pricing patterns in the Chicago Airbnb training data far more effectively than either neural network architecture.

- NN Model 1 and NN Model 2 exhibit very similar training performance across all metrics, with training MAE values of 55.35 and 57.31 and training R2 Scores of 0.5784 and 0.5714 respectively, suggesting that the additional regularization layers introduced in the second model did not result in any meaningful improvement in training set performance compared to the simpler first model.

- The most notable characteristic of NN Model 2 is the extremely small gap between its training and test metrics across all evaluation criteria, with a training MAE of 57.31 and a test MAE of 57.25, a training RMSE of 86.79 and a test RMSE of 86.85, and a training R2 Score of 0.5714 compared to a test R2 Score of 0.5617, collectively demonstrating that the regularization mechanisms in the second model have successfully minimized overfitting and produced the most consistent generalization behavior among all three models.

- NN Model 1 shows a moderate train-to-test performance gap with the R2 Score dropping from 0.5784 on training to 0.5356 on the test set, indicating a mild degree of overfitting that is considerably less severe than what the XGBoost model exhibits, where the R2 Score drops from 0.8431 on training to 0.6166 on the test set.

- The XGBoost model achieves the best test R2 Score of 0.6166 and the lowest test MSE of 6,598.25 among all three models, confirming that despite its larger train-to-test gap it still generalizes better than both neural network models in absolute terms and remains the strongest overall performer for predicting unseen Airbnb listing prices in Chicago.

- The test MAPE values of 36.13%, 38.67%, and 35.91% for NN Model 1, NN Model 2, and the XGBoost model respectively are relatively close to one another, indicating that all three models produce proportionally similar levels of relative prediction error on unseen data, and that none of the models achieves a decisive advantage in terms of percentage-based accuracy on the test set.

- Across all three models, the Adjusted R2 Scores closely track their corresponding R2 Scores on both the training and test sets, consistently confirming that the 23 input features including structured listing attributes and NLP-derived sentiment, topic, and zero-shot cl

## ***Insights, Business Recommendations and Conclusion***

***`Insights:`***

- Physical property attributes including the number of guests accommodated, bedrooms, and bathrooms are the most dominant predictors of Airbnb listing prices in Chicago, collectively accounting for the majority of the model's predictive signal and confirming that property size and capacity are the primary value drivers in the short-term rental market.

- NLP-derived features including LDA topic scores, zero-shot classification scores for cleanliness, service, location, safety, and amenities, and transformer-based sentiment scores all contribute meaningful predictive signal to the price prediction models, validating the core hypothesis that guest review content carries latent pricing information beyond what structured listing attributes alone can provide.

- The room type has a significant influence on pricing, with private rooms commanding considerably lower prices than entire homes or apartments, reflecting the strong premium that guests place on exclusive and unshared access to a property in the Chicago Airbnb market.

- Topic modeling reveals that guest reviews consistently revolve around identifiable latent themes, and the prominence of topic-based features among the top predictors suggests that the qualitative dimensions of guest experience captured through LDA are directly linked to how hosts price their listings and how guests perceive value.

- The review scores rating feature contributes meaningfully to price prediction, indicating that listings with consistently higher guest satisfaction ratings are associated with higher price points, suggesting that quality of experience as perceived and reported by guests translates into measurable pricing power for hosts.

- The XGBoost model outperforms both neural network architectures on the test set across all absolute error metrics, demonstrating that gradient boosted decision trees are particularly well suited for tabular Airbnb pricing data where feature interactions are complex but the dataset size does not fully leverage the representational advantages of deep learning.

- The regularized second neural network model achieves the smallest train-to-test performance gap among all models, demonstrating that Batch Normalization, Dropout, L2 regularization, and early stopping are effective strategies for improving generalization in neural network-based pricing models applied to structured Airbnb data.

- The persistent gap between training and test performance across all three models suggests that high-priced listing outliers, neighborhood-level price heterogeneity, and seasonality factors not captured in the current feature set continue to limit the models' ability to fully generalize to unseen listings in the Chicago market.

---

***`Business Recommendations:`***

***For Airbnb:***

- Airbnb should develop a dynamic pricing guidance tool powered by gradient boosting models that incorporates both structured listing attributes and NLP-derived review indicators, enabling the platform to provide hosts with real-time, data-driven pricing recommendations tailored to the Chicago market and extensible to other cities.

- The platform should invest in enhancing its review analytics infrastructure by systematically applying topic modeling, sentiment analysis, and zero-shot classification to guest reviews at scale, transforming unstructured feedback into actionable quality indicators that can be surfaced to both hosts and prospective guests.

- Airbnb should integrate guest sentiment trends and topic-based performance scores into its search ranking and listing visibility algorithms, ensuring that listings with consistently positive guest experiences across cleanliness, service, location, and amenities are rewarded with greater exposure on the platform.

- The platform should develop neighborhood-level pricing benchmarks that account for property size, room type, and review-derived quality indicators, enabling more transparent and fair pricing comparisons that help guests make informed booking decisions and help hosts position their listings competitively.

***For Hosts:***

- Hosts should prioritize maximizing the accommodation capacity of their listings by optimizing the number of guests accommodated, bedrooms, and bathrooms, as these structural attributes are the strongest determinants of listing price and represent the most direct levers for increasing revenue potential in the Chicago market.

- Hosts seeking to command a price premium should focus on consistently delivering high quality guest experiences across the key dimensions identified through zero-shot classification, particularly cleanliness, service responsiveness, and location accessibility, as these factors directly influence review scores and sentiment indicators that contribute to pricing power.

- Hosts should actively monitor the thematic patterns emerging from their guest reviews using topic modeling insights, as the prominence of topic-based scores in the feature importance rankings suggests that guests who frequently discuss specific experiential themes such as comfort, communication, and neighborhood quality are providing signals that influence how the market values similar listings.

- Hosts with private room listings should recognize the significant pricing gap relative to entire home listings and consider whether property modifications that enable full home access could unlock a substantially higher price tier, given that room type is among the top four most important pricing features identified by the model.

***For Customers:***

- Guests seeking the best value for their budget should use review-derived quality indicators such as sentiment scores, cleanliness ratings, and service scores as filters when evaluating listings, as these NLP-based signals provide a more nuanced and comprehensive picture of expected guest experience than star ratings alone.

- Guests should pay attention to the latent themes discussed in reviews through topic patterns, as recurring positive themes around location convenience, host communication, and property cleanliness are reliable indicators of a high quality stay that may not always be fully reflected in a listing's numerical rating.

- Guests prioritizing affordability should consider private room listings, which the model identifies as commanding significantly lower prices than entire homes, while still evaluating sentiment and topic scores to ensure that the lower price point does not come at the expense of a satisfactory guest experience.

---

***`Conclusion:`***

This project successfully addressed both objectives of conducting a comprehensive guest experience analysis and developing predictive pricing models for Airbnb listings in Chicago, Illinois. Through the application of sentiment analysis, zero-shot classification, and LDA topic modeling on guest review data, unstructured textual feedback was systematically transformed into measurable indicators of guest experience across dimensions including cleanliness, service, location, safety, and amenities. These NLP-derived features were subsequently integrated with structured listing attributes to build and evaluate three machine learning and neural network models for price prediction.

The XGBoost model emerged as the best overall performer, achieving a test R2 Score of 0.6166 and a test RMSE of 81.23, outperforming both neural network models in absolute generalization performance. The feature importance analysis confirmed that physical property attributes such as accommodation capacity, bedrooms, and bathrooms are the primary determinants of listing price, while NLP-derived topic scores, review ratings, and zero-shot classification scores contribute meaningful secondary predictive signal, validating the hypothesis that guest review content carries latent pricing information beyond what structured features alone provide.

The second neural network model demonstrated the most stable generalization behavior with the smallest train-to-test performance gap among all models, highlighting the effectiveness of regularization strategies including Batch Normalization, Dropout, L2 regularization, and early stopping in mitigating overfitting for neural network-based pricing models. Collectively, the results of this project demonstrate that integrating NLP-derived guest experience indicators with structured listing attributes produces a richer and more informative feature space for Airbnb price prediction, and provides a scalable analytical framework that Airbnb, hosts, and guests can leverage to make more informed pricing, quality improvement, and booking decisions in the competitive Chicago short-term rental market.

---
"""